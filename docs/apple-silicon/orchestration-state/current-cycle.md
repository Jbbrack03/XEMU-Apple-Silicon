# Current Cycle

- Started: 2026-05-21 22:08 local.
- Owner: Claude Code (Hermes-supervised cycle 4 of task #16).
- Session goal: Task #16 — instrument the Metal sampler path for the swizzle-mipmap XBE's stride==44 draws, correlate sampler-state attribution with the known back-buffer targets, and pair that with targeted draw-RT evidence so the remaining intra-mip Q0 collapse is localized between sampler setup and publish/compose.
- Required reading before substantive work: `docs/apple-silicon/handoff.md` (latest task #16 banner), `docs/apple-silicon/orchestration-workflow.md`, `docs/apple-silicon/decision-log.md` (latest task #16 entry), `docs/apple-silicon/automation.md` (relevant Metal diag toggles), `docs/apple-silicon/orchestration-state/handoff-summary.md`.
- Exit criteria:
  A) Preferred: land a bounded diagnostic or fix slice that isolates the remaining Task #16 fault surface with durable evidence, passes required validation, syncs docs, clean-commits, and exits.
  B) Acceptable fallback: leave the repo clean with a sharper durable diagnosis, staged evidence, updated canonical docs, and the next exact experiment.
- Result: **Exit Option A achieved (durable diagnosis + validated diag tooling, no fix landed by design).** Cycle 4 traced the intra-mip Q0 collapse end-to-end. Per-cell sampler state is correct on Metal (cycle-3 narrowing upheld but one layer too downstream); the fragment-shader bordered-UV transform `(uv*64+4)/128` is correct under the GL convention; the Metal renderer's `decode_face_levels` is missing the `s.border` 2x-upload that `gl/texture.c:451-456` and `vk/texture.c:111` apply unconditionally and `mtl_get_cubemap_face_size:598` reserves bytes for — only `mtl/texture_pg.c:737`'s `crop_cubemap_border = s.cubemap && s.border && !f.linear` gate doubles, missing the non-cubemap-2D path. UV math (`(uv*64+4)/128 * 64 → texel [10..26]`) lands all four sub-quad UVs in Q0 [0..31] of the un-doubled MTLTexture → observed Q0 collapse. Secondary bug: XBE library `xbed_texture_bind_stage0` never sets `XBED_FMT_BORDER_SOURCE_BIT`, leaving bit 3 = 0 = `BORDER_SOURCE_TEXTURE`, which is what triggers the bordered-UV transform in the first place.
- Out of scope: unrelated renderer cleanup, Task #17 GL work, pushing to origin, broad harness sweeps unrelated to Task #16, **the actual fix** (renderer + XBE library — multi-file change; needs own slice + validation).
- Validation status: build clean (`ninja -C build qemu-system-i386` succeeds after touch of all three modified files); Codex `/codex-validate changes` ran and returned MAJOR ISSUES with 3 findings, all adopted in-slice (orchestration-state reconciliation, `automation.md` "four" → "five" streams, `texture_pg.c` comment trimmed). Commit lands as part of closure; do NOT push.
- Notes for supervision: artifact-backed updates landed in `claude-status.md`, `validation-status.md`, and `handoff-summary.md` (this file's facts are mirrored there for orchestrator consumption).
