# Handoff Summary

- Cycle 17 closed the local renderer-agnostic milestone; cycle 18 packaged the state in a separate doc-only checkpoint.
- Cycle 19 attempted a real-Xbox parity check via the cycle-15 v0.4 image-blit XBE; reproducibly produced zero `D:\image-blit-capture.bin` / `D:\image-blit-done.txt` across two runs; cycle-17's xemu-side `pass=8/8 mask=0xff` could not be cross-witnessed. `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on decision DEFERRED.
- Cycle 20 took Path A (image-blit instrumentation). Added 13 staged marker calls writing to `D:\image-blit-marker-NN-STAGE.txt`. Local xemu validation showed all 13 markers fire via host-log channel; every `fopen("D:\\…","wb")` returns NULL on the ISO mount (CD-ROM, read-only). Two real-Xbox runs (pre-Codex labels, post-Codex shortened labels) BOTH produced zero marker files; chainload→FTP-back stable at 22.4 s. Conservative reading: D:\ write-back blocked under `runxbe` SITE-EXEC chainload (cycle-19 hypothesis #1 elevated to leading).
- Cycle 21 took Path A.2 (cheapest single follow-up): re-route the 13 markers from `D:\` to `E:\Apps\image-blit\…` — the xbe-harness's existing `--ftp-collect /E/Apps/<id>` target. Local xemu-Metal validation across 4 boots: 52 marker host-log lines, all `e_mount=1`, zero `fopen-failed`. Single real-Xbox run produced a clear NEGATIVE answer: chainload→FTP-back 22.4 s (4th independent reproduction across cycle-19+cycle-20+cycle-21), `verdict.json status: ok`, FTP-collect retrieved exactly 1 file (`default.xbe` upload echo), ZERO `image-blit-marker-*` files. Cycle-19 hypothesis #1 demoted from "leading" to "insufficient as sole explanation."
- **Cycle 22 took Path A.3 (the cycle-21-identified cheapest single follow-up): provenance audit of `docs/apple-silicon/xbox-real-references/{pipeline-smoke,mirror,color-channel,depth-floor,controller-roundtrip}`. Doc/evidence-only audit; no code, no XBE rebuild, no real-Xbox runs.** Methodology: enumerate every artifact (six PNGs, zero metadata docs); git-log each capture file to its introducing commit; read the orchestrator + xbe-harness + oracle-agent `cmd_runxbe` source at those commits and at HEAD; cross-reference with the dashboard-transition commit `e74715cd71` (UnleashX switch at 2026-05-06 19:21 CDT).
- **Cycle-22 per-set verdicts (all HIGH confidence):**
  - **pipeline-smoke** (intro `aae0138565` 2026-05-06 15:32 CDT, pre-UnleashX-switch, XBMC4Gamers/SITE RunXBE): **runxbe path** (same `client.runxbe()` RPC → agent `cmd_runxbe` → `XLaunchXBE()` chainload).
  - **mirror, color-channel, depth-floor** (intro `823733f2e6` 2026-05-06 23:03 CDT, post-switch, UnleashX/SITE EXEC): **runxbe path** (same chainload AND same dashboard as cycle-21 image-blit witness).
  - **controller-roundtrip + mirror/composite** (intro `58bf218838` 2026-05-07 10:39 CDT, UnleashX/SITE EXEC): **runxbe path**. composite.png is the parallel MS2109 HDMI-capture leg from `capture-composite-reference.sh`; the XBE-side chainload is the same `XLaunchXBE()`.
- **Source-lineage finding (load-bearing).** The agent-side chainload primitive `XLaunchXBE(path)` at `scripts/apple-silicon/xbe-tests/oracle-agent/commands.c:344-380` is provably unchanged between earliest capture commit (`aae0138565`, 2026-05-06) and cycle-21 HEAD (`edee829e49`, 2026-05-22) modulo two cosmetic edits — path-arg parsing rework (2026-05-10 `81e36900ef`) and SMC fan-curve cleanup (2026-05-12 `959d24acb8`). Neither alters the kernel-call chainload. The orchestrator's `run_diag` body diff is comment-only between `823733f2e6` and HEAD. The xbe-harness's real-Xbox flow added two helpers since `823733f2e6` but the actual upload + `run-diag` invocation is unchanged.
- **Conservative interpretation (cycle 22).** Cycle-19 hypothesis #1 ("D:\\ remap mismatch / runxbe-SITE-EXEC chainload blocks witness-path file writes") is **fully INVALIDATED** — five reference captures produced via the same path show the launch path itself is NOT the blocker. Cycle-21 hypothesis #3 (FATX-driver / NT-mount state diverges between FTP-server-time and chainloaded-XBE-time) is invalidated for D:\\. The image-blit failure across cycle 19+20+21 is conclusively **image-blit-specific**. Leading hypothesis is now: image-blit crashes BEFORE its main() body executes the first `image_blit_marker(0, "program_entered")` call — i.e. CRT init, static-init, DllCharacteristics, or pre-main XBE thunking. This LEADS because: (a) marker-00 is the literal first line of main()'s body and never reaches disk; (b) the 22.4 s chainload→FTP-back gap is consistent across 5 reproductions and matches an XBE early-exit reboot pattern; (c) every comparator XBE under the same launch path writes its D:\\<id>-capture.bin successfully.
- `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated. The flag continues to ship opt-in, default OFF.
- Codex validation SKIPPED for cycle 22 under rule #15's "doc-only changes" carve-out (zero code changes; aggregate edits are markdown-only in canonical docs + orchestration-state). Full per-slice justification recorded in `validation-status.md` and the cycle-22 decision-log entry. No validation marker written.
- **Cycle 22 closure: planned commit covers** `docs/apple-silicon/handoff.md` (cycle-22 entry on top; cycle-17/19/20/21 entries preserved) + `docs/apple-silicon/decision-log.md` (cycle-22 entry above cycle-21; no supersession) + `docs/apple-silicon/orchestration-state/{current-cycle.md, claude-status.md, validation-status.md, handoff-summary.md}` (all four updated). No code or XBE binaries touched.
- **Next bounded slice (NOT promoted this cycle):**
  - **A.4 (now top-priority per rule #1).** Add a non-fopen witness to image-blit: write a few bytes into the oracle-agent's persistent kernel-pool controller buffer (`oracle_ctrl_buffer` at the agent-published phys address) BEFORE attempting any marker fopen. Next agent boot reads the buffer via `controller.buffer-info` / `controller.get`. Discriminates the cycle-22 leading hypothesis (#1, "image-blit crashes before main() first instruction") from the cycle-21 #2 framing (NV2A init / pbkit init crash). This is now the right next bounded slice because A.3 made the launch-path-blocker explanation untenable.
  - **B.** Smaller PFIFO-race-only Tier-1 diag XBE that captures via PCRTC. Still on the table; better suited if A.4 lands inconclusively.
  - **C (new, cycle 22).** If A.4's witness fires (image-blit DOES reach first-instruction execution), shift focus to xbed_init / pbkit-init / first NV2A call on real Xbox via per-section instrumentation. If A.4's witness does NOT fire, the crash is in CRT/static-init/XBE thunking — would need either a smaller image-blit variant or XBE-file-level analysis (DllCharacteristics, kernel imports, section layout).
- All evidence and canonical-doc updates are on disk; the next session can resume cold from files per the orchestration-workflow.md §7 model.
