# Handoff Summary

- **Cycle 41d (CLOSED 2026-05-24; closure commit on top of cycle-41c closure `e990fc2bf3`; bounded implementation+run slice):** Alignment-drop variation per cycle-41c closure's binding contingent path. Source change: 1 literal at `scripts/apple-silicon/xbe-tests/lib/xbed_self_witness.c:214` flipping the `MmAllocateContiguousMemoryEx` `Alignment` argument from `0x1000u` (cycles 29..41c) to `0u` (let the real-Xbox kernel pick natural page-granular alignment for contiguous memory). Cycle-41c matched-tuple address range (`lowest=0x00000000u, highest=0x7FFFFFFFu`) + `Protect = PAGE_READWRITE | PAGE_WRITECOMBINE` (cycle 41b) UNCHANGED. Plus NEW Codex-R1 P1-adopted page-alignment guard at `xbed_self_witness.c:317-346` — `if ((phys & 0xFFFu) != 0u) { distinct host-log + MmFreeContiguousMemory + return 0 }` — that rejects any returned `phys` not 0x1000-aligned, closing the cycle-41d-specific "kernel could in principle return sub-page-aligned phys that the cycle-29 consumer's 4 KiB stride cannot see" interpretation gap. In-tree usage precedent for `Alignment=0u` exists at `nxdk/lib/pbkit/pbkit.c:2297`, `nxdk/samples/{triangle,mesh,xaudio}/main.c`, and `xbe-tests/flat-tri-depth/main.c:77`; the nxdk header at `xboxkrnl.h:3464` is a bare prototype with no `Alignment=0` doc text — comment/header explicitly note "we cannot point to a documented 'kernel picks natural page alignment' guarantee; what the in-tree calls demonstrate is only that the API accepts `0u`, not what alignment the kernel returns." Codex round-1 P3 low "stale cycle-41c guard log strings" ADOPTED via cycle-41d relabel at `xbed_self_witness.c:296` + `:307`. Comment block at `xbed_self_witness.c:120-220` rewritten + header `xbed_self_witness.h:101-148` Safety-notes block updated. Cycle-29 first-call branch / EEPROM-write breadcrumb / sticky `s_eeprom_scratch_attempted` gate / `MmPersistContiguousMemory` / `wbinvd` stamp / `phys | 0x80000000` cached-mirror alias for the readback path: ALL UNCHANGED. Cycle-23 lockstep + cycle-29 self-witness + cycle-31 paint + cycle-35 `.CRT$X*` slot + cycle-39 EEPROM-scratchpad path + cycle-41a + cycle-41b comment tightening + cycle-41c symmetric phys-range guards: ALL PRESERVED. **Codex 3-round review** (mode=`changes`): R1 MAJOR ISSUES (P1 high "alignment-drop blind spot on consumer 4 KiB stride" ADOPTED via page-alignment guard; P3 low "stale cycle-41c log strings" ADOPTED via cycle-41d relabel) → R2 MINOR ISSUES (R1.P1+P3 confirmed addressed via in-diff guard + relabel; one low comment-precision finding already satisfied by in-source soft framing at `xbed_self_witness.c:175-189` + `xbed_self_witness.h:107-119`) → R3 GREEN (Codex explicit "Current source already satisfies the R2 low finding... No further wording softening is needed. This slice is deploy-ready for the real-Xbox run."). All R1 P1+P3 findings ADOPTED. Codex marker at `.claude/state/codex-validate-last-run` refreshed. Clean nxdk rebuild produced new witness-only artifact deployed-build SHA = `c49ca0ad2a3f968ecb0ea02bf16a7117da614b912ff98feb58ed925ee387f589` (155 648 B; SHA varies between rebuilds of identical source because XBE/COFF embeds build timestamps; source bit-identical to codex-validated GREEN state; oracle-agent unchanged at cycle-39 v0.5 SHA `d419b452…`). **OUTCOME: G0(c) PERSISTS under alignment-drop** per the cycle-40 G-row discriminator table. Post-chainload signals identical to cycles 40 + 41a + 41b + 41c: EEPROM byte at 0xFF = `0xA4` + `witness.scan-self count=0 mapped_pages_seen=419` + `witness.scan = count=1 phys=0x03eb3000 reserved=0` (D-cycle-27 shape). Full `eeprom` hex dump cross-check confirmed last byte = `A4`. `MmAllocateContiguousMemoryEx(0x1000, 0x00000000, 0x7FFFFFFF, 0u, PAGE_READWRITE | PAGE_WRITECOMBINE)` STILL did NOT yield a usable allocation on this real-Xbox kernel. **Cycle-41d eliminates the page-alignment requirement (cycle-22 candidate "alignment requirement `0x1000`") as the failing constraint.** Combined with cycle 40 + 41a + 41b cache-policy exhaustion + cycle 41c address-range elimination, **the cycle-22 leading hypothesis is FURTHER NARROWED**: the failing constraint is one of {the `-Ex` variant itself, a `size=0x1000`-specific interaction}. Dashboard FTP recovery at t+19s post-chainload (within sampling variance vs cycle 41c t+24s; recovery timing NOT load-bearing for G-row classification). **Deployment sequence executed (18 logged steps under `benchmark-runs/cycle41d-alignment-20260524T054838Z/`, gitignored per project convention):** 00 baseline scans MET (ping=true, agent=true v0.5 resident from cycle 41c); 01 pre-baseline `eeprom.scratch.read` byte=0xA4 (cycle-41c leftover); 02 reboot 1 at 06:00:02Z → 03 dashboard FTP back at t+20s; 04 cycle-41d witness-only SHA captured; 05 FTP-upload witness-only with `--overwrite` (uploaded=1); 06 ensure-agent (v0.5 re-launched); 07 unsafe.enable; 08 eeprom.scratch.reset → byte=0x00 confirmed; 09 eeprom.scratch.read confirmed byte=0x00 baseline; 10 witness.scan-self count=0; 11 witness.scan D-cycle-27 phys=0x03eb3000; 12 runxbe at 06:03:09Z; 13 dashboard FTP back at t+19s; 14 ensure-agent post-chainload; 15 final witness.scan D-cycle-27 phys=0x03eb3000; 16 final witness.scan-self count=0; 17 final eeprom.scratch.read byte=0xA4; 18 full eeprom hex dump cross-check last byte = A4. Composite capture SKIPPED with cycle-34+36+41a+41b+41c silent-stall rationale; cycle-41d primary + secondary signals are fully agent-side. **Reproducibility wins:** `phys=0x03eb3000` deterministic kernel-pool reuse REPRODUCED across baseline + post-deployment + final readbacks (24+ consecutive observations across cycles 26..41d); `mapped_pages_seen=419` REPRODUCED at every readback; EEPROM non-volatility across the pre-chainload reboot 1 CONFIRMED. **Recommended cycle 41e (Hermes's call):** non-`-Ex` fallback variation — replace `MmAllocateContiguousMemoryEx(0x1000u, 0x00000000u, 0x7FFFFFFFu, 0u, PAGE_READWRITE | PAGE_WRITECOMBINE)` with the standard 2-arg `MmAllocateContiguousMemory(0x1000u, PAGE_READWRITE | PAGE_WRITECOMBINE)`. Bounded ~3-line source change. All other cycle-41d preserved invariants stay UNCHANGED. If cycle 41e also fails G0(c), only fundamentally different approaches remain (custom XBE-header callback before `_start` — high scope; requires `nxdk/tools/cxbe/` changes; OR redesign of cycle-29 self-witness as multi-page — changes the WTNS layout contract). Cycle-40 G0(c) signal remains the regression gate. **ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-39 v0.5 verbs intact; cycle-41d alignment-drop is PRODUCER-side only — consumer scan window unchanged); ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json touched; ZERO `nxdk/` source touched (read-only inspection only of `pbkit.c`, `hal/video.c`, `xboxkrnl.h`, samples for the `Alignment=0u` precedent search); ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched.** Pre-existing tracked drift in 4 `scripts/apple-silicon/*.{sh,py}` PRESERVED unstaged per the rolling cycle-34+ Hermes-supervision pattern (carried forward through cycles 35..41d); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 22+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-41e+ non-`-Ex`-fallback work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle 41c + 41b + 41a + 40 + 39 + 38 + 37 + 36 + 35 entries preserved unchanged below.

- **Cycle 41c (CLOSED 2026-05-24; closure commit on top of cycle-41b closure `c3f3d19932`; bounded implementation+run slice):** Combined address-range variation per cycle-41b closure's binding contingent path. Source change: 2 literals at `scripts/apple-silicon/xbe-tests/lib/xbed_self_witness.c:155-156` widening the `MmAllocateContiguousMemoryEx` address range from `lowest=0x00010000, highest=0x03FFFFFF` (cycles 29..41b) to `lowest=0x00000000, highest=0x7FFFFFFF` — matching nxdk's framebuffer allocator at `nxdk/lib/hal/video.c:363-367` BYTE-FOR-BYTE modulo `size` (`screenSize` differs only because nxdk's framebuffer is multi-page; cycle-41c is single-page). Alignment `0x1000` + `Protect = PAGE_READWRITE | PAGE_WRITECOMBINE` UNCHANGED from cycle-41b. Plus NEW Codex-round-1+round-2 P1-adopted symmetric phys-range guards at `xbed_self_witness.c:254-273` (lower: `phys < 0x00010000u`, upper: `phys >= 0x04000000u`) that reject any kernel return outside the cycle-29 consumer's scan window `[0x00010000, 0x04000000)`; each guard host-logs distinctly, `MmFreeContiguousMemory(p)`, and returns 0. Comment block at `xbed_self_witness.c:120-180` rewritten + header `xbed_self_witness.h:101-130` Safety-notes block updated. Cycle-29 first-call branch / EEPROM-write breadcrumb / sticky `s_eeprom_scratch_attempted` gate / `MmPersistContiguousMemory` / `wbinvd` stamp / `phys | 0x80000000` cached-mirror alias for the readback path: ALL UNCHANGED. Cycle-23 lockstep + cycle-29 self-witness + cycle-31 paint + cycle-35 `.CRT$X*` slot + cycle-39 EEPROM-scratchpad path + cycle-41a comment tightening + cycle-41b comment block: ALL PRESERVED. **Codex 3-round review** (mode=`changes`): R1 MAJOR ISSUES (P1 high "high-phys blind spot in cycle-29 consumer alignment" ADOPTED via upper guard; P2 med "overstatement" ADOPTED via retail-64MiB-scope qualification; P2 med "doc-rule-#4 mid-slice state" DEFLECTED — transient, closure commit syncs; P3s confirming) → R2 MAJOR ISSUES (P1 high "symmetric low-phys blind spot" ADOPTED via lower guard; P2 med "still-too-strong elimination" ADOPTED via further-narrowed "kernel demands specific non-cycle-29-tuple address range" wording) → R3 MINOR ISSUES (P2 med "count=0 overstated as 'allocation failed'" ADOPTED via wording softening; Codex explicit "Nothing else looks load-bearing for cycle-41c real-Xbox deploy"). All R1+R2 P1 + R3 P2 findings ADOPTED. Codex marker at `.claude/state/codex-validate-last-run` refreshed (fingerprint `cc437e2b`). Clean nxdk rebuild produced new witness-only artifact SHA = `cc437e2b7da250fe18be120da168e973a72a3f81559a60d99d5e2e7bc6034175` (155 648 B; oracle-agent unchanged at cycle-39 v0.5 SHA `d419b452…`). **OUTCOME: G0(c) PERSISTS under matched-tuple address range** per the cycle-40 G-row discriminator table. Post-chainload signals identical to cycles 40 + 41a + 41b: EEPROM byte at 0xFF = `0xA4` + `witness.scan-self count=0 mapped_pages_seen=419` + `witness.scan = count=1 phys=0x03eb3000 reserved=0` (D-cycle-27 shape). Full `eeprom` hex dump cross-check confirmed last byte = `A4`. `MmAllocateContiguousMemoryEx(0x1000, 0x00000000, 0x7FFFFFFF, 0x1000, PAGE_READWRITE | PAGE_WRITECOMBINE)` — the EXACT nxdk framebuffer allocator tuple modulo `size` — STILL did NOT yield a usable allocation on this real-Xbox kernel. **Cycle-41c eliminates the "kernel demands specific non-cycle-29-tuple address range" sub-hypothesis** (the matched-tuple is known-good against the same `-Ex` entry point for nxdk's framebuffer allocator on this kernel). Combined with cycle 40 + 41a + 41b cache-policy exhaustion, **the cycle-22 leading hypothesis is FURTHER NARROWED**: the failing constraint is one of {alignment requirement `0x1000`, the `-Ex` variant itself, a `size=0x1000`-specific interaction}. Dashboard FTP recovery at t+24s post-chainload (IDENTICAL to cycle 41b; +18s vs cycle-40 baseline; recovery timing NOT load-bearing for G-row classification). **Deployment sequence executed (18 logged steps under `benchmark-runs/cycle41c-addressrange-20260524T051200Z/`, gitignored per project convention):** 00 baseline scans MET (ping=true, agent=true v0.5 resident from cycle 41b); 01 pre-baseline `eeprom.scratch.read` byte=0xA4 (cycle-41b leftover); 02 reboot 1 at 05:28:17Z → 03 dashboard FTP back at t+24s; 04 new cycle-41c witness-only SHA captured; 05 FTP-upload witness-only with `--overwrite`; 06 ensure-agent (v0.5 re-launched); 07 unsafe.enable; 08 eeprom.scratch.reset; 09 eeprom.scratch.read confirmed byte=0x00; 10 witness.scan-self count=0; 11 witness.scan D-cycle-27; 12 runxbe at 05:29:28Z; 13 dashboard FTP back at t+24s; 14 ensure-agent post-chainload; 15 final witness.scan D-cycle-27; 16 final witness.scan-self count=0; 17 final eeprom.scratch.read byte=0xA4; 18 full eeprom hex dump cross-check last byte = A4. Composite capture SKIPPED with cycle-34+36+41a+41b silent-stall rationale; cycle-41c primary + secondary signals are fully agent-side. **Reproducibility wins:** `phys=0x03eb3000` deterministic kernel-pool reuse REPRODUCED across baseline + post-deployment + final readbacks (23+ consecutive observations across cycles 26..41c); `mapped_pages_seen=419` REPRODUCED at every readback; EEPROM non-volatility across the pre-chainload reboot 1 CONFIRMED. **Recommended cycle 41d (Hermes's call):** alignment-drop variation — change `xbed_self_witness.c` `alignment` argument from `0x1000u` to `0u` (let the kernel pick alignment); bounded single-literal change. If cycle 41d still fails G0(c), only non-`-Ex` fallback to plain `MmAllocateContiguousMemory(0x1000)` (cycle 41e) remains in cycle-41 scope. Cycle-40 G0(c) signal remains the regression gate. **ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-39 v0.5 verbs intact; cycle-41c phys-range guards are PRODUCER-side only); ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json touched; ZERO `nxdk/` source touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched.** Pre-existing tracked drift in 4 `scripts/apple-silicon/*.{sh,py}` PRESERVED unstaged per the rolling cycle-34+ Hermes-supervision pattern (carried forward through cycles 35..41c); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 22+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-41d+ alignment / `-Ex`-fallback work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle 41b + 41a + 40 + 39 + 38 + 37 + 36 + 35 entries preserved unchanged below.

- **Cycle 41b (CLOSED 2026-05-24; closure commit on top of cycle-41a closure `69866b94a5`; bounded implementation+run slice):** Second cache-policy candidate from the cycle-41a closure's ranked next-step list. Source change: 1 line at `scripts/apple-silicon/xbe-tests/lib/xbed_self_witness.c:158` flipping the `MmAllocateContiguousMemoryEx` `Protect` argument from `PAGE_READWRITE | PAGE_NOCACHE` (cycle-41a) to `PAGE_READWRITE | PAGE_WRITECOMBINE` (0x04 | 0x200 → 0x04 | 0x400) + paired comment block rewrite at `xbed_self_witness.c:125-152` + header `xbed_self_witness.h:101-118` Safety-notes block update to document the cycle-41b divergence + the new precedent (nxdk's framebuffer allocator at `nxdk/lib/hal/video.c:363-367` uses `PAGE_READWRITE | PAGE_WRITECOMBINE` against `MmAllocateContiguousMemoryEx` — symmetric known-good site to cycle-41a's OHCI NOCACHE precedent). Cycle-29 first-call branch / EEPROM-write breadcrumb / sticky `s_eeprom_scratch_attempted` gate / `MmPersistContiguousMemory` / `wbinvd` stamp / `phys | 0x80000000` cached-mirror alias for the readback path: ALL UNCHANGED. Cycle-23 lockstep + cycle-29 self-witness shim + cycle-31 paint sequence + cycle-35 `.CRT$X*` slot mechanism + cycle-39 EEPROM-scratchpad path + cycle-41a Codex-adopted comment tightening: ALL PRESERVED. **Codex round-1 v3 review** (mode=`changes`, verdict=**GREEN**): v1 attempt got stuck in a `web_search` loop the read-only sandbox cannot service; v3 with explicit "DO NOT use web search; nxdk lives at `../nxdk/...`" guidance produced GREEN in 41,456 tokens with 3 P3 confirming findings: (P3.1) no hard-rule conflicts; (P3.2) `PAGE_NOCACHE=0x200` + `PAGE_WRITECOMBINE=0x400` distinct non-overlapping bits per `nxdk/lib/xboxkrnl/xboxkrnl.h:3294`; precedent verified at `nxdk/lib/hal/video.c:363`; (P3.3) no new coherency hazard since the producer-side page-zero + header-stamp + counter-increment + wbinvd all operate through the kseg0 cached-mirror alias `phys | 0x80000000` (canonicalized at `xbed_self_witness.c:165`) not through the kernel-returned `p` whose page-table entry carries the WC bit. Codex marker at `.claude/state/codex-validate-last-run` refreshed. Clean nxdk rebuild produced new witness-only artifact SHA = `fbd828a24edcac95278fb62d486a951a0c5ba9c48454a27c32032ef3959f8404` (155 648 B; oracle-agent unchanged at cycle-39 v0.5 SHA `d419b452…`). **OUTCOME: G0(c) PERSISTS under `PAGE_WRITECOMBINE`** per the cycle-40 G-row discriminator table. Post-chainload signals identical to cycles 40 + 41a: EEPROM byte at 0xFF = `0xA4` + `witness.scan-self count=0 mapped_pages_seen=419` + `witness.scan = count=1 phys=0x03eb3000 reserved=0` (D-cycle-27 shape). Full `eeprom` hex dump cross-check confirmed last byte = `A4`. `MmAllocateContiguousMemoryEx(0x1000, 0x10000, 0x3ffffff, 0x1000, PAGE_READWRITE | PAGE_WRITECOMBINE)` STILL did NOT yield a usable allocation on this real-Xbox kernel. **Cycle-41b eliminates `PAGE_WRITECOMBINE` as a working cache-policy fix.** Combined with cycle-41a's elimination of `PAGE_NOCACHE` + cycle-40's elimination of bare `PAGE_READWRITE`, **CACHE-POLICY VARIATIONS ARE EXHAUSTED**. Dashboard FTP recovery at t+24s post-chainload (+16s vs cycle-41a t+8s; +18s vs cycle-40 t+6s; still faster than cycle-36 t+38s graceful; +16s shift NOT dismissable as sampling variance but recovery timing NOT load-bearing for G-row classification). The cycle-22 leading hypothesis is FURTHER NARROWED: the failing constraint is NOT a cache-policy bit at all — it is one of {address-range floor `0x00010000`, address-range ceiling `0x03FFFFFF`, alignment `0x1000`, the `-Ex` variant itself}. **Deployment sequence executed (18 logged steps under `benchmark-runs/cycle41b-pagewritecombine-20260524T043727Z/`, gitignored per project convention):** 00 baseline scans MET (ping=true, agent=true v0.5 resident from cycle 41a); 01 pre-baseline `eeprom.scratch.read` byte=0xA4 (cycle-41a leftover); 02 reboot 1 → 03 dashboard FTP back at t+18s; 04 new cycle-41b witness-only SHA captured; 05 FTP-upload witness-only with `--overwrite`; 06 ensure-agent (v0.5 re-launched); 07 unsafe.enable; 08 eeprom.scratch.reset; 09 eeprom.scratch.read confirmed byte=0x00; 10 witness.scan-self count=0; 11 witness.scan D-cycle-27; 12 runxbe at 044438Z; 13 dashboard FTP back at t+24s; 14 ensure-agent post-chainload; 15 final witness.scan D-cycle-27; 16 final witness.scan-self count=0; 17 final eeprom.scratch.read byte=0xA4; 18 full eeprom hex dump cross-check last byte = A4. Composite capture SKIPPED with cycle-34+36+41a silent-stall rationale; cycle-41b primary + secondary signals are fully agent-side. **Reproducibility wins:** `phys=0x03eb3000` deterministic kernel-pool reuse REPRODUCED across baseline + post-deployment + final readbacks (22+ consecutive observations across cycles 26..41b); `mapped_pages_seen=419` REPRODUCED at every readback; EEPROM non-volatility across the pre-chainload reboot 1 CONFIRMED. **Recommended cycle 41c (Hermes's call):** combined address-range candidate matching the framebuffer-allocator address range exactly (`lowest=0x00000000, highest=0x7FFFFFFF, alignment=0x1000, Protect = PAGE_READWRITE | PAGE_WRITECOMBINE`) — closest possible match to a known-good nxdk-side call; two-line code change at `xbed_self_witness.c:155-156`. If cycle 41c still fails G0(c), only alignment-drop (cycle 41d) and non-`-Ex` fallback (cycle 41e) remain in scope. Cycle-40 G0(c) signal remains the regression gate. **ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-39 v0.5 verbs intact); ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json touched; ZERO `nxdk/` source touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched.** Pre-existing tracked drift in 4 `scripts/apple-silicon/*.{sh,py}` PRESERVED unstaged per the rolling cycle-34+ Hermes-supervision pattern (carried forward through cycles 35..41b); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 21+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-41c+ address-range variation work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle 41a + 40 + 39 + 38 + 37 + 36 + 35 entries preserved unchanged below.

- **Cycle 41a (CLOSED 2026-05-24; closure commit on top of cycle-40 closure `df92fb3306`; bounded implementation+run slice):** Lowest-scope cycle-41 candidate from the cycle-40 closeout's ranked low-scope-first list. Source change: 1 line at `scripts/apple-silicon/xbe-tests/lib/xbed_self_witness.c:138` flipping the `MmAllocateContiguousMemoryEx` `Protect` argument from bare `PAGE_READWRITE` to `PAGE_READWRITE | PAGE_NOCACHE` (0x04 → 0x04 | 0x200) + Codex-adopted comment-only tightening at `xbed_self_witness.c:127-145` (Codex P1 medium "comment overstated kseg0+NOCACHE alias semantics") + `xbed_self_witness.h:101-118` Safety-notes block rewrite (Codex P2 low "header doc still says RW protection matches agent"). Cycle-29 first-call branch / EEPROM-write breadcrumb / sticky `s_eeprom_scratch_attempted` gate / `MmPersistContiguousMemory` / `wbinvd` stamp / `phys | 0x80000000` cached-mirror alias for the readback path: ALL UNCHANGED. Cycle-23 lockstep + cycle-29 self-witness shim + cycle-31 paint sequence + cycle-35 `.CRT$X*` slot mechanism + cycle-39 EEPROM-scratchpad path: ALL PRESERVED. Precedent for `PAGE_READWRITE | PAGE_NOCACHE` against `MmAllocateContiguousMemoryEx` exists in nxdk itself at `nxdk/lib/usb/libusbohci_xbox/usbh_xbox.c:35-41` (OHCI DMA-coherent ring buffers — known-good nxdk-side site that uses the `-Ex` variant). Codex round-1 (mode=`changes`, verdict=**MINOR ISSUES**): P1 + P2 ADOPTED via comment-only updates; alias-change suggestion (`phys | 0xB0000000` end-to-end uncached-alias variant) DEFLECTED as out-of-slice scope (cycle-41a is one bounded variation; alias-change is a separate candidate); open question about WRITECOMBINE resolved by Codex itself ("more natural later experiment, not an obvious requirement for this specific witness page") — filed as cycle-41b candidate; out-of-scope finding "agent's own allocator at `oracle-agent/controller.c:221-248` still uses plain `PAGE_READWRITE`" NOTED but not actioned (agent buffer empirically works across many chainloads since cycle 23; not in the failing kernel-state region); hard-rule conflicts: NONE. Codex marker at `.claude/state/codex-validate-last-run` refreshed. Clean nxdk rebuild via `eval $(/Users/jbbrack03/XEMU_MacOS/nxdk/bin/activate -s) && make NXDK_DIR=/Users/jbbrack03/XEMU_MacOS/nxdk` produced new witness-only artifact SHA = `7dae8cf9cc08c60f699628eb284a0b1e93b7d8ac580af6851b55b3ed6bb08f78` (155 648 B; oracle-agent unchanged at cycle-39 v0.5 SHA `d419b452…`). **OUTCOME: G0(c) PERSISTS under `PAGE_NOCACHE`** per the cycle-40 G-row discriminator table. Post-chainload signals identical to cycle 40: EEPROM byte at 0xFF = `0xA4` (tag=0xA, stage_nib=0x4 — cycle-39 self-witness pre-MmAlloc breadcrumb landed; sticky-flag gate enforced) + `witness.scan-self count=0 mapped_pages_seen=419` + `witness.scan = count=1 phys=0x03eb3000 reserved0=0 reserved1=0` (D-cycle-27 shape). Full `eeprom` hex dump cross-check confirmed last byte = `A4`. `MmAllocateContiguousMemoryEx(0x1000, 0x10000, 0x3ffffff, 0x1000, PAGE_READWRITE | PAGE_NOCACHE)` STILL did NOT yield a usable allocation on this real-Xbox kernel — the kernel rejects (NULL-return or internal crash) the cycle-29 allocation tuple even with the `PAGE_NOCACHE` cache-policy bit added. **Cycle-41a eliminates `PAGE_NOCACHE` as a working cache-policy fix.** Dashboard FTP recovery at t+8s post-chainload (cycle 40 was t+6s; +2s within sampling variance; still anomalously fast vs cycle-36 t+38s clean recovery; STILL consistent with watchdog hardware reset rather than `HalReturnToFirmware` graceful exit). Cycle-22 leading hypothesis FURTHER STRENGTHENED but not yet narrowed beyond cycle-40's narrowing: at least one of {`PAGE_WRITECOMBINE` cache-policy, address-range floor/ceiling combination, page-alignment requirement, the `-Ex` variant itself} is the failing constraint. **Deployment sequence executed (18 logged steps under `benchmark-runs/cycle41a-pagenocache-20260524T040940Z/`, gitignored per project convention):** 00 baseline scans MET (ping=true, agent=true v0.5 resident from cycle 40); 01 pre-baseline `eeprom.scratch.read` byte=0xA4 (cycle-40 leftover); 02 reboot 1 → 03 dashboard FTP back at t+6s; 04 new cycle-41a witness-only SHA captured; 05 FTP-upload witness-only with `--overwrite`; 06 ensure-agent (v0.5 re-launched); 07 unsafe.enable; 08 eeprom.scratch.reset; 09 eeprom.scratch.read confirmed byte=0x00; 10 witness.scan-self count=0; 11 witness.scan D-cycle-27; 12 runxbe at 041123Z; 13 dashboard FTP back at t+8s; 14 ensure-agent post-chainload; 15 final witness.scan D-cycle-27; 16 final witness.scan-self count=0; 17 final eeprom.scratch.read byte=0xA4; 18 full eeprom hex dump cross-check last byte = A4. Composite capture SKIPPED with cycle-34+36 silent-stall rationale; cycle-41a primary + secondary signals are fully agent-side. **Reproducibility wins:** `phys=0x03eb3000` deterministic kernel-pool reuse REPRODUCED across baseline + post-deployment + final readbacks (21+ consecutive observations across cycles 26..41a); `mapped_pages_seen=419` REPRODUCED at every readback; EEPROM non-volatility across the pre-chainload reboot 1 CONFIRMED. **Recommended cycle 41b (Hermes's call):** `PAGE_WRITECOMBINE` cache-policy candidate (symmetric one-line change in `xbed_self_witness.c:138`). If WRITECOMBINE also fails identically, cache-policy variations are exhausted → cycle 41c broadens to address-range / alignment / non-`-Ex` fallback per the ranked-low-scope-first list in cycle-41a SUMMARY.md. Cycle-40 G0(c) signal remains the regression gate (EEPROM byte must stay `0xA4` + watchdog-reset-shape recovery; a SUCCESSFUL variation should advance `witness.scan-self count >= 1`). **ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-39 v0.5 verbs intact); ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json touched; ZERO `nxdk/` source touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched.** Pre-existing tracked drift in 4 `scripts/apple-silicon/*.{sh,py}` PRESERVED unstaged per the rolling cycle-34+ Hermes-supervision pattern (carried forward through cycles 35..41a); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 19+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-41b+ allocation-flag variation work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle 40 + 39 + 38 + 37 + 36 + 35 entries preserved unchanged below.

- **Cycle 40 (CLOSED 2026-05-24; closure commit on top of cycle-39 closeout-sync `ca66cfe652`; bounded run-only slice):** Real-Xbox EEPROM scratchpad discriminator run executing the cycle-39 11-step runbook from `witness-only/README.md` cycle-39 addendum. ZERO source / script / nxdk / host xemu / lib edits this cycle; deployed XBE binaries are exactly the cycle-39 Codex-validated builds (witness-only SHA `7528bb5bf4c9cc8f00c934c38e015166de7a6f89236ddc8e2ad2195a47ce4e0d`, oracle-agent SHA `d419b452f127e5a065a51b2d5bba49b6f2ec1f1826cdec57845793c34440cb53`). **OUTCOME: G0(c)** per the cycle-40 G-row discriminator table. Reads as `(EEPROM 0xFF=0xA4, witness.scan-self count=0, witness.scan = D-cycle-27)`: `eeprom.scratch.read` returned `off=0xFF byte=0xA4 tag=0xA stage_nib=0x4 interp="cycle-39 self-witness pre-MmAlloc breadcrumb (sub-case (c) if WTNS count=0; G1..G4 if WTNS count>=1)"` (cross-confirmed by full `eeprom` hex dump — last byte at offset 0xFF = `A4`); `witness.scan-self` = `count=0 mapped_pages_seen=419`; `witness.scan` = `count=1 phys=0x03eb3000 reserved0=0 reserved1=0 mapped_pages_seen=419` (D-cycle-27 shape consistent across cycles 26..40). **The cycle-39 EEPROM-write code DID execute on real Xbox up to AND including the `HalWriteSMBusValue(0xA8, 0xFF, FALSE, 0xA4)` call inside `xbed_self_witness_fire`'s first-call branch (positioned AS THE LAST INSTRUCTION before `MmAllocateContiguousMemoryEx`); the subsequent `MmAllocateContiguousMemoryEx(0x1000, 0x10000, 0x3ffffff, 0x1000, PAGE_READWRITE)` call did NOT yield a usable allocation (returned NULL silently or crashed inside, leaving `s_witness_page` NULL and `count=0`).** Sub-cases G0(a) "crash inside nxdk's pre-`.CRT$X*` startup" and G0(b) "crash inside `_witness_only_pre_main_crt_xx`'s body BEFORE the pre-MmAlloc instruction" are ELIMINATED by the EEPROM byte landing at `0xA4`. The cycle-22 leading hypothesis is FURTHER NARROWED from "pre-main crash, anywhere" to specifically "`MmAllocateContiguousMemoryEx(0x1000, 0x10000, 0x3ffffff, 0x1000, PAGE_READWRITE)` returns NULL silently OR crashes inside on real-Xbox kernel for the cycle-29 allocation tuple". γ.0 narrowing is finer-grained but still strictly pre-`main()`-completion; γ.1 remains INVALIDATED. **Dashboard FTP recovery time t+6s** (4 seconds of ping=false at t+2/t+4, then ping+ftp+auth=OK at t+6s) — anomalously fast vs cycle-36 t+38s; consistent with kernel-detected allocation crash → watchdog hardware reset rather than `HalReturnToFirmware` graceful exit (Sleep(2000) + reboot path alone is at least 3-5 s of guest time). **Sequence executed** (15 logged steps under `benchmark-runs/cycle40-real-xbox-eeprom-discriminator-20260524T021727Z/`, gitignored per project convention): 00 reachability + baseline scans MET (ping=true, agent=true v0.4 resident from cycle 39); 01 reboot 200 (1st — to bring up dashboard FTP); 02 FTP recovery at t+~14s with auth OK; 03 FTP-upload cycle-39 oracle-agent v0.5 + ensure-agent (banner cosmetically still "v0.4" — cycle-39 only updated the source banner-comment, not the runtime `cmd_info` string; the new verbs `eeprom.scratch.read` + `eeprom.scratch.reset` are present and work correctly); 04 EEPROM scratchpad baseline armed via `unsafe.enable` + `eeprom.scratch.reset` → byte 0x00 confirmed; 05 reboot 200 (2nd — deviation from runbook step order to release dashboard FTP for witness-only upload; EEPROM non-volatile so baseline survived); 06 FTP-upload cycle-39 witness-only XBE `--overwrite`; 07 ensure-agent + re-verify preconditions (EEPROM 0xFF still 0x00; both scans match baseline); 08 composite capture SKIPPED with documented rationale (cycle-34 + cycle-36 reproduced ffmpeg silent-stall; cycle-40 primary signal is EEPROM byte which is fully agent-side and reliable); 09 first runxbe attempt with wrong syntax (`runxbe E:\…`) rejected by agent (`usage: runxbe path=<xbox-path>`); 10 second runxbe with correct syntax (`runxbe path=E:\Apps\witness-only\default.xbe`) at 022351Z; 11 dashboard FTP recovery poll → back at t+6s; 12 ensure-agent re-launch; 13 final `witness.scan` D-cycle-27 + `witness.scan-self` count=0; 14 `eeprom.scratch.read` returns `byte=0xA4` + full eeprom dump cross-check confirms last byte = `A4`. **Reproducibility wins:** `phys=0x03eb3000` deterministic kernel-pool reuse REPRODUCED across 4 readbacks this session (baseline + post-deploy + post-witness-upload + post-chainload) and 20+ consecutive observations across cycles 26..40; `mapped_pages_seen=419` REPRODUCED at every readback; EEPROM non-volatility across two soft reboots within this session CONFIRMED. **Secondary findings filed for cycle 41+ (NOT cycle-40 scope):** (i) cycle-39 oracle-agent `cmd_info` runtime banner still reads "v0.4" despite v0.5 verbs working (1-line cosmetic fix); (ii) `runxbe` verb requires explicit `path=` named-argument syntax (already correctly documented). **Recommended cycle 41** (Hermes's call; binding per cycle-39 closure's contingent path for G0(c)): explore `MmAllocateContiguousMemoryEx` allocation-flag variations in `lib/xbed_self_witness.c:133-138`. Candidates low→high scope: (1) cache policy `PAGE_NOCACHE` / `PAGE_WRITECOMBINE`; (2) broaden address range to lowest=0 / highest=0xFFFFFFFF; (3) lower alignment to 0; (4) fall back to non-`-Ex` variant. Cycle-40 G0(c) is the cycle-41 regression gate: EEPROM byte must stay `0xA4` (sticky-flag guarantee); a successful variation should advance `witness.scan-self count >= 1`. Each variation ~5-LOC + 1 rebuild + 1 redeploy + 1 run. DEFERRED beyond cycle 41 (only if multiple variations all fail): custom XBE-header callback (RULED OUT for G0(c) per cycle 40 evidence; may resurface if cycle-41 variations all fail in ways suggesting fundamentally unusable kernel surface). **ZERO host xemu source touched; ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-39 EEPROM-write + Codex round-2 sticky-flag gate intact); ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `oracle-agent/*` touched (cycle-39 v0.5 verbs intact); ZERO `witness-only/main.c` / Makefile / manifest.json touched; ZERO `nxdk/` source touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched.** **Codex SKIPPED per rule #15 run-only / doc-only carve-out** — ZERO uncommitted code diff in xemu-fork scope this cycle; the deployed binaries are unchanged from cycle 39 closure; cycle-39 marker at `.claude/state/codex-validate-last-run` remains the relevant marker for the deployed artifacts. Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the rolling Hermes-supervision pattern (carried forward from cycles 34..39); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 18+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the same pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-41+ allocation-flag variation work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED.

- **Cycle 39 (CLOSED 2026-05-23; closure commit `33fb5b7e34`; bounded closeout-sync follow-up commit on top syncs orchestration-state quartet to reference the landed hash; bounded implementation slice):** EEPROM scratchpad pre-`MmAllocateContiguousMemoryEx` discriminator. Ships the cycle-38-recommended Option C dynamic discriminator that uniquely splits G0 sub-case (c) "`MmAllocateContiguousMemoryEx` returned NULL silently" (post-run EEPROM byte at offset `0xFF` = `0xA4` + `witness.scan-self count=0`) from sub-cases (a)+(b) "crash before reaching the allocation call" (byte=`0x00` + count=0). Surface: 1 new sticky static + ~50-LOC code block in `lib/xbed_self_witness.c` inside the existing first-call branch positioned BEFORE `MmAllocateContiguousMemoryEx`, gated AT MOST ONCE per process via a NEW `s_eeprom_scratch_attempted` flag (Codex round-2 P1 finding adopted — without the sticky gate, a G0(c) outcome would re-fire on later `.CRT$XCU`/in-main calls and OVERWRITE the breadcrumb byte from `0xA4` to `0xA5`/`0xA1`/`0xA3`, destroying the discriminator value); 3 new `#define`s + ~85-line cycle-39 head-comment addendum in `lib/xbed_self_witness.h`; two new agent verbs `eeprom.scratch.read` (4-branch decode that explicitly classifies non-`0xA4` `0xA?` values as "indeterminate" — Codex round-1 P2 finding adopted — rather than aliasing onto sub-case (c)) + `eeprom.scratch.reset` (gated by existing `unsafe.enable`) in `oracle-agent/{commands.c,commands.h,main.c}`; banner-comment `v0.5 eeprom.scratch.*` addendum; ~118-LOC cycle-39 README addendum (scratchpad contract table + cycle-40 G-row discriminator table + cycle-40 11-step deployment runbook + design-choice rationale); manifest title extension + `real-xbox/physical/cycle-40` expected_results section. EEPROM offset 0xFF rationale: documented as part of the 0xC0..0xFF reserved/unused tail of the standard Xbox EEPROM layout (consistently zero on stock OEM consoles; does NOT touch the factory/encrypted region 0x00..0x5F, user-settings region 0x60..0x96, or user-settings-checksum region 0x96..0xBF); 24LC02 endurance ≥1M write cycles vs ~1 write/cycle-39+ run. Cycle-23 lockstep contract intact (no `xbed_a4_witness.{c,h}` change); cycle-29 self-witness shim allocation/stamp/wbinvd flush path intact (new EEPROM write is ADDITIVE and positioned BEFORE the allocation; existing post-allocation paths structurally unchanged); cycle-31 paint sequence intact (witness-only/main.c unchanged); cycle-35 `.CRT$X*` slot mechanism intact. **Build:** both XBEs rebuilt cleanly with ZERO new lld warnings — `witness-only/bin/default.xbe` 155 648 B SHA-256 `7528bb5bf4c9cc8f00c934c38e015166de7a6f89236ddc8e2ad2195a47ce4e0d`; `witness-only.iso` 720 896 B SHA-256 `d2c2089913267215f51d5fb24d050c20482e859cda72cf6a7c9370fb7e626bd4`; `oracle-agent/bin/default.xbe` 417 792 B SHA-256 `d419b452f127e5a065a51b2d5bba49b6f2ec1f1826cdec57845793c34440cb53`; `oracle-agent/oracle-agent.iso` 983 040 B SHA-256 `283b11d3b201eb8ff1480e602c2701314fdd3ea7d6163792a7d3dbeeaa2d9cca`. **Codex 3 rounds per rule #15** (non-trivial diff > 30 LOC; ~975-line aggregate including ~85 LOC lib/ source + ~90 LOC agent source + ~118 LOC README + paired headers/comments): R1 = MAJOR ISSUES (1 P2 reader-decode finding adopted; rebuilt oracle-agent). R2 = MAJOR ISSUES (1 P1 sticky-flag finding adopted; rebuilt witness-only). R3 = MAJOR ISSUES (1 P1 about pre-existing tracked drift in `retail-*.py` importing untracked `composite_preflight.py` — DEFLECTED as out of cycle-39 scope per the rolling cycle-34..38 Hermes-supervision guardrail; cycle-39 closing commit does NOT stage those files; cycle-39 source surface itself had no findings in round 3). Codex marker written at `.claude/state/codex-validate-last-run` with round-3 fingerprint. **Local validation envelope:** cold-boot xemu smoke (`dist/xemu.app -display none -nographic` against `witness-only.iso` with `XEMU_GUEST_LOG=1`) did NOT reach the witness-only XBE within the bounded slice's ~120 s wait budget — cold boot through BIOS to DVD load via the .app wrapper exceeds that envelope without a pre-warmed snapshot path (the cycle-35 closure's "12 s spawn" must have depended on a snapshot mechanism not available in this bounded session); structural correctness coverage substituted by (i) clean nxdk lld link of both XBEs with zero new warnings, (ii) the cycle-29 shim's existing Codex-validated first-call branch where the new EEPROM write is inserted as a sticky-flag-gated additive instruction sequence, (iii) xemu's QEMU `smbus-eeprom` device implementing both `eeprom_receive_byte` and `eeprom_write_data` per `hw/i2c/smbus_eeprom.c:52-83` so the SMBus write IS honored in emulation when reached (ruling out "broken in xemu" failure mode), (iv) Codex 3-round source review. Real discriminator answer lives in cycle 40 (real-Xbox run + post-run `eeprom.scratch.read`). **Hypothesis state:** unchanged from cycle 38 closure — three live G0 sub-cases (a/b/c) remain, instrumentation now in place for cycle-40 readback to split (c) from (a)+(b); cycle-22 pre-main-crash hypothesis remains FULLY CORROBORATED; γ.1 remains INVALIDATED. **Recommended cycle 40** (Hermes's call): deploy cycle-39 oracle-agent (v0.5) + cycle-39 witness-only on real Xbox via the 11-step cycle-40 runbook in `witness-only/README.md` (steps 3a `unsafe.enable` + `eeprom.scratch.reset` baseline + 11 `eeprom.scratch.read` recovery bracket the existing cycle-36 chainload window). Cycle 41 branches conditional on cycle-40 outcome: G0(c) → allocation-flag variations; G0(a)+(b) → custom XBE-header callback (high scope; only fund if forced). **ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/lib.mk` touched; ZERO `lib/xbed_runtime.{c,h}` touched; ZERO `oracle-agent/{controller,smc,tier2,protocol}.{c,h}` touched; ZERO image-blit / pipeline-smoke / mirror / other diag XBE touched; ZERO `witness-only/main.c` touched (new code picked up via the cycle-35 `.CRT$X*` slot path automatically); ZERO `witness-only/Makefile` touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched; ZERO `nxdk/` source touched.** Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the rolling Hermes-supervision pattern (carried forward from cycles 34..38); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 17+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the same pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-40+ real-Xbox EEPROM-readback discrimination + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED.

- **Cycle 38 (CLOSED 2026-05-23; bounded tooling-only slice):** lld link-map symbol resolution for the cycle-37 unexplained `.text 0x16720` walker fn-ptr. Executes the cycle-37 closeout's recommended next step (re-link witness-only with lld's map output and resolve the symbol). ONE bounded rebuild (witness-only only) with `LDFLAGS=-map:<path>` appended via env (lld-link PE/COFF map flag syntax — NOT GNU ld's `-Map=`); identical `.obj` inputs (none changed since cycle 35); resulting binary structurally bit-identical to cycle-35 modulo 8 timestamp bytes total (verified via `cmp -l`: 6 bytes differ in `default.xbe` across 3 separate 2-byte spans = three XBE timestamp embeddings; 2 bytes differ in `main.exe` at one COFF timestamp; `.text` / `.rdata` / `.data` / `.tls` content bit-identical). Post-rebuild: tracked `bin/default.xbe` + `witness-only.iso` restored via `git checkout --`; gitignored `main.exe` restored from local backup; post-restore SHA-256s match cycle-35 originals exactly (`ab52df8d…` / `a8033fef…` / `6f4ecd59…`). **Result: concrete positive symbol-resolution result.** Five numbered findings filed: **F38.1** — `.text 0x16720` = `_automount_d_drive` from `libnxdk_automount_d:automount_d.obj` (lld map's `Publics by Value` table: `0001:00005720 _automount_d_drive 0000000000016720 libnxdk_automount_d:automount_d.obj`); source registers `automount_d_drive_p` in `.CRT$XIT` via `__attribute__((section(".CRT$XIT"), used))` (`nxdk/lib/nxdk/automount_d.c:35`). **F38.2** — `.CRT$XIT` is nxdk default, NOT witness-only opt-in: `nxdk/lib/nxdk/Makefile:38-41` force-includes `_automount_d_drive` for every nxdk XBE that does NOT set `NXDK_DISABLE_AUTOMOUNT_D=y`; neither witness-only / mirror / pipeline-smoke sets that flag; all three nxdk XBEs include `_automount_d_drive`. **F38.3** — Full CRT walker subsection layout enumerated: `_PDCLIB_xbox_run_pre_initializers` walks `.CRT$XXA..XXZ` invoking 3 functions in order — `_witness_only_pre_main_crt_xx` (cycle-35; at `.text 0x11000`), `_fls_init` (nxdk default `libwinapi:fiber.obj`; at `.text 0x2bd00`), `_tsc_freq_init` (nxdk default `libwinapi:profiling.obj`; at `.text 0x2d950`); `_PDCLIB_xbox_run_crt_initializers` walks `.CRT$XIA..XIZ` invoking 1 function — `_automount_d_drive` (nxdk default; at `.text 0x16720`) — then walks `.CRT$XCA..XCZ` invoking 1 function — `_witness_only_pre_main_crt_xc` (cycle-35; at `.text 0x11040`); total 5 walker invocations; net delta vs a baseline non-witness nxdk XBE = exactly +2 walker entries (the two cycle-35 slots), matching cycle 35's design. **F38.4** — Cycle-37 dump entries past `.CRT$XXZ` are NOT walker entries: VAs `0x3303c` onward resolve to `.text` addresses inside `libpdclib:malloc.obj` (`_dlmalloc_set_footprint_limit + 100`, `_internal_mallinfo + 256`, `_internal_mallinfo + 572`) — most likely `.rdata`-resident pdclib malloc constants / vtables that the cycle-37 zero-delimited-group heuristic mis-classified as function pointers because their values happen to fall in the `.text` VA range. **F38.5** — Cycle-37 F5 hypothesis REVISED: the "+1 unexplained walker group" was a heuristic-counting artifact of how the cycle-37 Python XBE parser delineated zero-bounded fn-ptr runs in the merged `.CRT=.rdata` region; the actual semantic CRT walker entry count delta vs mirror is exactly +2 (the two cycle-35 slots; XCU + XXC) — NOT +1 walker group + +2 fn-ptrs. **Net verdict.** The lld link-map analysis yields a concrete positive symbol-resolution result that REVISES (NOT confirms) the cycle-37 F5 hypothesis. The static binary surface of `witness-only` is now confirmed structurally identical to mirror modulo the 2 cycle-35 walker entries; there is **NO witness-only-unique pre-`.CRT$X*` code path** that mirror's pre-main slate does not exercise. The G0 real-Xbox crash narrows further to one of three sub-cases (cycle-37 F5 "+1 walker group drag-in" alternative ELIMINATED): (a) crash inside nxdk's pre-`.CRT$X*` startup (`_start` / `__security_init_cookie` / TLS-size / `_PDCLIB_xbox_libc_init`); (b) crash inside `_witness_only_pre_main_crt_xx`'s body BEFORE `MmAllocateContiguousMemoryEx`; (c) `MmAllocateContiguousMemoryEx` returns NULL silently. γ.0 narrowing is finer-grained but still pre-`main()`; cycle-22 leading hypothesis remains FULLY CORROBORATED; γ.1 remains INVALIDATED (cycle 36 G0 outcome). **Recommended cycle-39 next step** (lowest-cost highest-information): cycle-36 Option C — EEPROM scratchpad write inside `xbed_self_witness_fire` BEFORE `MmAllocateContiguousMemoryEx`, paired with agent EEPROM read-back via the `unsafe.enable` + EEPROM-write surface. Uniquely discriminates (c) from (a)+(b). Other candidates considered and rejected: `out 0xe9` host-log breadcrumb (invisible on real Xbox — SKIP); `.CRT$XCV` slot between `.CRT$XCU` and `main()` (adds no new info beyond cycle-35 stage-4 fire — SKIP). **ZERO source/script code edits this cycle; ZERO net XBE binary change vs cycle-35; ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched; ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json / README.md touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` touched; ZERO `nxdk/` source touched (lld map READ `nxdk/lib/nxdk/automount_d.c` + `nxdk/lib/nxdk/Makefile` + `nxdk/lib/pdclib/platform/xbox/crt_initializers.c` as read-only inspection only).** **Codex SKIPPED per rule #15 trivial-work carve-out** (slice did not produce any non-trivial code change; one rebuild was a tooling-only `-map`-flag add and the deployed-binary bytes were restored to the cycle-35 Codex-validated artifact; the cycle-35 binary observed bit-identically this cycle is the cycle-35 build that passed 4-round Codex green at cycle-35 closure commit `515e03f4e7`). Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the rolling Hermes-supervision pattern (carried forward from cycle 37); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 14+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the same pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-39+ EEPROM-scratchpad work OR alternative pre-`.CRT$X*` discriminator + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED.

- **Cycle 37 (CLOSED 2026-05-23; bounded analysis-only slice):** Static XBE binary diff — `witness-only` (cycle-35 build, 155 648 B, SHA-256 `ab52df8dee...`) vs `pipeline-smoke` (110 592 B; pure nxdk, no `lib/lib.mk`; PNG SHA-256 matches `expected.py:default()` byte-for-byte per `.claude/rules/oracle-and-xbe.md`) + `mirror` (147 456 B; uses `lib/lib.mk` — same shared runtime as witness-only; passed xemu-Metal vs math-derived oracle and boot-functional in prior oracle-pipeline runs). Mirror is the strictest comparison; the source-side delta vs witness-only is just main.c text + `lib/xbed_self_witness.c` opt-in + the two cycle-35 `.CRT$X*` slots. **Methodology**: purpose-built Python XBE parser (`benchmark-runs/cycle37-static-xbe-diff-20260523T232828Z/xbe_parse.py`; field offsets cross-checked against Caustik's XBE spec + nxdk `tools/cxbe`; satisfies project rule #5 "build tools rather than substitute weaker evidence"). Three diff axes: (1) XBE header / section / TLS structure; (2) kernel thunk-table imports (decoded with debug XOR key `0xEFB1F152` / retail key `0x5B6D40B6` per XBE spec); (3) `.CRT$X*` static-initializer walker arrays in `.rdata` (merged from `.CRT*` subsections via `#pragma comment(linker, "/merge:.CRT=.rdata")` in `nxdk/lib/pdclib/platform/xbox/crt_initializers.c`). **Result: narrowed-but-not-conclusive negative result.** Six numbered findings filed: **F1** — XBE header / section / library / init-flag / entry-point-key bitwise-identical across all three XBEs (witness-only is NOT malformed at the XBE-header level). **F2** — TLS layout bit-identical across all three (vsize=276 raw=4 callback=NULL zerofill=0 datarange=0x110) — RULES OUT sub-case (a)'s "TLS-size computation crash". **F3** — Kernel imports witness-only ≡ mirror (78 ordinals same set); the 6-ordinal delta vs pipeline-smoke {47, 100, 137, 156, 168, 173} is fully accounted for by `lib/lib.mk`; NO kernel function imported by witness-only is missing from mirror; the "`xbed_self_witness.c` introduces a new kernel call that crashes pre-main" hypothesis is UNSUPPORTED — all four `Mm*` calls (`MmAllocateContiguousMemoryEx`, `MmPersistContiguousMemory`, `MmGetPhysicalAddress`, `MmFreeContiguousMemory`) used by `xbed_self_witness.c` are already imported and used by `xbed_runtime.c` in mirror. **F4** — Cycle-35 slots physically present in witness-only binary (strings `0x22237` + `0x221f9` + `_PDCLIB_xbox_run_crt_initializers` at `0x20903`; `main.obj` has `.CRT$XXC` + `.CRT$XCU` 4-byte sections; helper functions + slot variables in `main.obj` symbol table); slots did NOT get dead-code-eliminated. **F5 (only structural drift surfaced)** — CRT walker layout: pipeline-smoke and mirror each have 4 walker groups (`[1, 2, 1, 2]` fn-ptrs); witness-only has 5 walker groups (`[1, 1, 3, 1, 2]` fn-ptrs). Net delta witness-only vs mirror: +1 walker group AND +2 fn-ptrs in another group. The +2 matches cycle 35's 2 new slots cleanly; the +1 EXTRA WALKER GROUP is NOT directly accounted for by cycle 35. The singleton fn-ptr in the extra group points at `.text 0x16720`. Candidate sources (UNVERIFIED in this slice): nxdk auto-link of an additional CRT-subsection contributor — `nxdk/lib/winapi/profiling.obj` + `nxdk/lib/winapi/fiber.obj` define `.CRT$XXT`; `nxdk/lib/nxdk/automount_d.obj` defines `.CRT$XIT`; `xbed_self_witness.c` may drag one of these in via symbol reference. **F6** — Cycle-35 binary DOES boot on xemu per cycle-35 closure local-smoke transcript (all 4 expected WTNS fires emitted in correct order under `XEMU_GUEST_LOG=1`); G0 crash is REAL-XBOX-ONLY; static XBE bytes load identically on both hosts (no load-time relocations); divergence is in either (i) nxdk's pre-`.CRT$XX*` startup code (`_start` / `__security_init_cookie` / TLS setup / `_PDCLIB_xbox_libc_init`) reacting differently to real-Xbox BIOS / kernel context tolerated by xemu, OR (ii) `MmAllocateContiguousMemoryEx`'s real-Xbox behavior diverging from xemu's emulation, OR (iii) the not-yet-identified `.text 0x16720` symbol faulting on real Xbox. **Net verdict**: four cycle-36-listed G0 candidate failure modes (malformed XBE header / TLS-size crash / kernel-import mismatch / dead-code-eliminated slots) RULED OUT; one new narrowing question filed (`.text 0x16720` symbol identity). Cycle-22 pre-main-crash hypothesis FULLY CORROBORATED (per cycle 34) and FURTHER STRENGTHENED with three concrete narrower sub-hypotheses replacing the earlier general G0 sub-cases (a)/(b)/(c) enumeration. **Recommended cycle-38 next step** (cheapest, highest-information): re-link existing witness-only build with lld's `--print-map` output (`-Wl,-Map=witness-only.map`) and resolve which symbol corresponds to the `.text 0x16720` singleton fn-ptr. Tooling-only change (one extra linker flag); no source edits, no slot additions, no rebuild of any shared lib. Either eliminates the +1 walker-group hypothesis (benign nxdk-default initializer) OR identifies the next instrumentation target. If negative, cycle-39 fallback is cycle-36 Option C (EEPROM scratchpad write inside `xbed_self_witness_fire` before `MmAllocateContiguousMemoryEx`) which uniquely discriminates sub-case (c) from (a)/(b) but adds substantial scope. **ZERO source/script code edits this cycle; ZERO XBE rebuilds; ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched; ZERO `xbed_runtime.{c,h}` touched; ZERO `witness-only/main.c` / Makefile / manifest.json / README.md touched; ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` touched; ZERO `nxdk/` source touched (READ-only inspection of `crt_initializers.c` + per-obj `.CRT$X*` enumeration only).** **Codex SKIPPED per analysis/doc-only carve-out** (rule #15 not triggered — slice produced no non-trivial code change). The cycle-35 binary observed bit-identically this cycle is the cycle-35 build that passed 4-round Codex green at cycle-35 closure commit `515e03f4e7`. Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the rolling Hermes-supervision pattern (carried forward from cycle 36); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 13+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the same pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-38+ map-file analysis OR cycle-39 EEPROM-scratchpad fallback + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED.

- **Cycle 36 (CLOSED 2026-05-23; closure commit `265010549f`; bounded closeout-sync follow-up commit on top syncs orchestration-state quartet):** Real-Xbox discriminator run for the cycle-35 pre-main breadcrumb build. Bounded RUN-ONLY / DOC-ONLY slice; ZERO source/script/XBE edits; ZERO host xemu source touched. **OUTCOME G0** per the cycle-35 G-row discriminator table (`witness-only/README.md` cycle-35 addendum): reads as `(stripes visible = none, witness.scan-self count = 0, witness.scan = D-cycle-27)`. `witness.scan = count=1 phys=0x03eb3000 reserved0=0 reserved1=0 mapped_pages_seen=419` identical to cycles 28/30/32/34; `witness.scan-self = count=0` — **NO WTNS page allocated → not even `.CRT$XXC` slot (stage=4) ran**; 26-snap NTSC composite burst over t+0..t+29.5s of the second runxbe shows ZERO stripe colors (13/26 pure black = composite-signal-off frames; 13/26 dashboard transition/return frames including pre-runxbe snap_00 = 25 874 unique colors and dashboard-return snaps 20-22 = 27k..40k unique colors confirming capture path healthy). G0 means the crash occurred BEFORE the `.CRT$XXC` slot fired — strictly earlier than the first user-C code the nxdk CRT runs in this XBE. G1/G2/G2'/G3/G4 ALL require `count >= 1` → G0 ELIMINATES all of them. F-row interpretation from the cycle-32 F-table is moot (every F row assumes `main()` was at least attempted). Three pre-`.CRT$XXC`-fire sub-cases share this G0 shape and cycle-35 evidence CANNOT distinguish them on real Xbox (no host-log breadcrumb): (a) crash inside `_start` / `__security_init_cookie` / TLS-size computation / `_PDCLIB_xbox_libc_init` — strictly earlier than anything cycle 34 could distinguish; (b) walker invoked `.CRT$XXC` slot but helper body crashed BEFORE `MmAllocateContiguousMemoryEx`; (c) `MmAllocateContiguousMemoryEx` returned NULL silently from `.CRT$XXC` (edge case in `lib/xbed_self_witness.c:54-74`). **Hypothesis state update:** (γ) "main() never reaches the fire calls" FURTHER STRENGTHENED — even `.CRT$XXC` (the first user-C code) did not run; cycle-22 pre-main-crash hypothesis NARROWED beyond cycle-34's F4 to pre-`.CRT$XXC` window; **γ.1 ("`XVideoSetMode` faulted before returning") INVALIDATED** (the cycle-35 slot fires strictly BEFORE main() enters; if main() had even started, count >= 1 would have been observed); (α) and (β) FURTHER DEPRIORITIZED (both presuppose main() ran the cycle-23 fires); hypothesis #5 UNCHANGED PARTIALLY INVALIDATED in the catastrophic-hang sense (cycle 36 reproduced clean dashboard recovery in t+~26..38s across both runxbe attempts). **Sequence executed** (16 logged steps under `benchmark-runs/cycle36-real-xbox-witness-only-pre-main-discriminator-20260523T225306Z/`, gitignored per project convention): 00 reachability (ping=true ftp=false agent=true — cycle-29 agent resident from cycle 34); 01 ensure-agent baseline (already listening); 02 baseline both scans MET (D-cycle-27 + count=0); 03 reboot 200; 04 poll dashboard FTP (curl verbose LIST 226 confirms ready); 05 FTP-upload cycle-35 `witness-only/bin/default.xbe` (SHA-256 `ab52df8dee32c24b857b3df749e3b8fc0a5a7e8f0949e06ec5c7e4d82aaef5bd`, 155 648 B) `--overwrite` to `/E/Apps/witness-only/default.xbe` (uploaded=1 skipped=0 failed=0; remote mtime advanced Dec 10 17:17 → Dec 11 02:20 = cycle-31 → cycle-35 file actually replaced on disk); 06 ensure-agent + recheck scans (preconditions still MET); 07 composite-preflight (`status=ok` via xemu-capture in 1.759 s); 08 composite-record.sh `--duration 80` (preflight OK; ffmpeg launched; SILENT-STALLED for 103 s, ffmpeg_rc=137 capture_timed_out=true, ZERO bytes stderr/video.mp4 — **cycle-34 finding (i) reproduced in a SEPARATE physical power session**, upgrading the finding from "one-off cycle-32 + cycle-34" to "reproducible across power sessions"); 09 first `runxbe` issued 23:02:38Z; 10 dashboard FTP back 23:03:16Z = t+38s (clean recovery); 11 ensure-agent + final scans (first run) = (D-cycle-27, count=0) — already enough to land G0; 12 xemu-capture snap_00 pre-runxbe (capture path healthy, 25 874 unique colors); 13 second `runxbe` issued 23:06:43Z (cycle-34-style snapshot-burst substitution because composite-record.sh failed at step 8); 14 25-snap burst over t+0..t+29.5s with explicit `--width 720 --height 480`; 15 stripe analysis = ZERO stripe colors detected across all 26 snaps; 16 ensure-agent + final scans (second run) = (D-cycle-27, count=0) — REPRODUCED. **Reproducibility wins:** `witness.scan-self count=0` reproduced across two runxbe attempts in same physical power session; `witness.scan = D-cycle-27` reproduced across 5 readbacks this session (baseline + post-relaunch + post-first-runxbe + post-second-runxbe + post-burst-window) AND ≥6 cycles (26/28/30/32/34/36); kernel-pool deterministic `phys=0x03eb3000` reuse ≥20+ consecutive observations across cycles 26..36 now across at least two physical power sessions; `mapped_pages_seen=419` reproduced at every readback. **Secondary findings filed for cycle 37+ consideration (NOT cycle-36 scope):** (i) **composite-record.sh ffmpeg silent-stall REPRODUCED in cycle 36** (already-filed cycle-34 finding (i) upgraded to "reproducible across at least two physical power sessions"; the cycle-33 preflight correctly reports ok via xemu-capture but does NOT protect the subsequent ffmpeg long-form recording from the TCC inheritance asymmetry; cycle 37+ candidates: extend composite-preflight.sh with `--require-both-detectors` OR have composite-record.sh always run a brief ffmpeg liveness check before arming the full duration); (ii) **G0-vs-edge-case-(c) ambiguity** documented in the G0 row itself — cycle 37 candidate: instrument `xbed_self_witness.c` with a 4-byte EEPROM scratchpad write BEFORE `MmAllocateContiguousMemoryEx` to discriminate (c) from (a)/(b). **ZERO source/script code edits this cycle; ZERO XBE rebuilds; ZERO host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-27 preserve gate + cycle-29 `witness.scan-self` verb intact); ZERO `xbed_runtime.{c,h}` touched; ZERO image-blit touched; ZERO `witness-only/main.c` / Makefile / README.md / manifest.json touched (cycle-35 sources intact); ZERO `tools/xemu-capture/` source touched; ZERO `scripts/apple-silicon/composite-record.sh` / `composite-preflight.sh` source touched (cycle-33 implementations intact).** **Codex SKIPPED under rule #15 doc-only / run-only carve-out** (same path as cycles 26/28/30/32/34); the cycle-35 binary observed this cycle is bit-identical to the cycle-35 build that passed 4-round Codex green at cycle-35 closure commit `515e03f4e7`; cycle-35 marker at `.claude/state/codex-validate-last-run` remains the relevant marker for the deployed artifact. Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the rolling Hermes-supervision pattern (carried forward from cycle 35); pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 12+ pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the same pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-37+ pre-`.CRT$XXC` discriminator work + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED; cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated; flag continues to ship opt-in, default OFF. **Cycle-37+ scope (Hermes's call)** per the cycle-35 README G0-row "Next" column: (1) custom XBE-header callback that runs before nxdk's `_start` (requires modifying nxdk's `tools/cxbe/` XBE-header generator — uniquely distinguishes sub-case (a)); (2) static binary diff against a known-good nxdk XBE (`pipeline-smoke` or `mirror`); (3) EEPROM-scratchpad write inside `xbed_self_witness_fire` BEFORE `MmAllocateContiguousMemoryEx` (uniquely distinguishes (c) from (a)/(b); adds substantial scope).

- **Cycle 35 (CLOSED 2026-05-23; closure commit `515e03f4e7`):** Path A.4 pre-main breadcrumb via `.CRT$X*` static-init slots — implementation-only bounded code slice. Cycle 34's F4 outcome (cycle-22 pre-main-crash hypothesis FULLY CORROBORATED in strongest form) could not distinguish γ.0 (`main()` never entered AT ALL) from γ.1 (`XVideoSetMode` itself faulted before returning). Cycle 35 adds the cheapest mechanism that runs strictly BEFORE `main()`: two new function-pointer slots in nxdk's CRT-initializer sections that each call `xbed_self_witness_fire(stage)` with brand-new pre-main stage codes — `.CRT$XXC` (stage=4) walked by `_PDCLIB_xbox_run_pre_initializers()` from `WinMainCRTStartup`'s entry thread AFTER `__security_init_cookie` + TLS setup + `_PDCLIB_xbox_libc_init` but BEFORE `thrd_create(main_wrapper)`; `.CRT$XCU` (stage=5) walked by `_PDCLIB_xbox_run_crt_initializers()` from `main_wrapper`'s thread immediately BEFORE `main()`. The fires reuse the existing cycle-29 `xbed_self_witness_fire` shim (idempotent: first call allocates + zeroes + magic/version-stamps the WTNS page; subsequent calls stamp + tick counter). After a fully successful run the page carries `reserved0=0xA4000003` (last stamp = in-`main()` POST_MARKER0) AND `reserved1=4` (2 pre-main + 2 in-`main()` WTNS fires); the cycle-36 `witness.scan-self` `reserved1` counter becomes the load-bearing γ.0-vs-γ.1 discriminator signal. **Design choice rationale (full table in `witness-only/README.md` cycle-35 addendum):** Option (1) `.CRT$X*` static-init slot stamp **CHOSEN** — mechanism documented + exercised on every nxdk-built XBE; ZERO nxdk / linker / XBE-header changes; ZERO new shared-lib code (reuses cycle-29 fire); scope = `witness-only/main.c` only. Option (2) custom XBE-header callback **REJECTED** — no documented "pre-CRT entry slot" in nxdk's `tools/cxbe/`; implementation would require modifying nxdk itself, widening scope beyond `witness-only`; γ.0 sub-windows it could uniquely distinguish (`_start` / `__security_init_cookie` / TLS-size computation crashes) are vanishingly unlikely cycle-22 hang sites. Option (3) thinner alternative to `XVideoSetMode` via direct NV2A CRTC register writes **REJECTED** — does NOT address γ.0-vs-γ.1 (if `main()` does not enter at all, no in-`main()` code runs regardless); widens NV2A surface. **Implementation surface entirely contained in `scripts/apple-silicon/xbe-tests/witness-only/`:** `main.c` cycle-35 head-comment addendum (~100 LOC) + 2 new locally-defined stage constants (4, 5; deliberately NOT in `lib/xbed_a4_witness.h` to keep cycle-23 lockstep namespace untouched) + 2 static `_PVFV`-shaped helper functions + 2 `__attribute__((section(".CRT$X*C"), used))` function-pointer slot declarations. ZERO changes to existing `main()` body: cycle-31 stripe paint sequence + cycle-25 host-log line + cycle-23 XCTR fires + cycle-29 in-`main()` WTNS fires + cycle-31 final `Sleep(2000)` + `HalReturnToFirmware(HalRebootRoutine)` all unchanged. **Build:** `witness-only/bin/default.xbe` 155 648 B (UNCHANGED from cycle 31; new code fits within existing nxdk XBE page boundary); `witness-only.iso` 720 896 B (unchanged); benign `lld: warning: .edata=.rdata: already merged into .edataxb` repeats prior cycles; ZERO new warnings. **Local xemu smoke validation PASS** (12 s timeout spawn against `witness-only.iso` with `XEMU_GUEST_LOG=1`): host-log emitted, in expected order, all 4 WTNS fires + correct counter progression `1 → 2 → 3 → 4`; both `.CRT$X*` slots fire BEFORE `main()` enters; cycle-29 shim's idempotent same-page reuse holds across all 4 calls (single WTNS page at phys=0x03fdf000, counter ticks correctly with the right stage byte at each fire). **Discriminator scope:** cycle 35 is positioned narrowly as a γ.0-sub-window discriminator. A cycle-36 readback's `reserved1` counter narrows the crash site: count=0 → G0 (`_start` / `__security_init_cookie` / TLS / `_PDCLIB_xbox_libc_init` crash; strictly earlier than cycle-34's F4; OR `MmAllocateContiguousMemoryEx` returned NULL from `.CRT$XXC` — edge case); count=1 reserved1=1 → G1 (`thrd_create` or `.CRT$XI*` crash); count=1 reserved1=2 → G2 (γ.1 **candidate** window, NOT corroborated — TWO sub-cases share this shape and cycle-35 cannot distinguish them: γ.0-sub `main()` never entered after `.CRT$XCU` OR γ.1 `main()` entered and crashed inside paint(0) = `XVideoSetMode`; cycle 37 should add a `.CRT$XCV` slot to separate); count=1 reserved1=3..4 + no stripes visible → **G2'** (cycle-35 analogue of cycle-32 F4' — graceful `XVideoSetMode` FALSE; `main()` continued through in-`main()` WTNS fires; γ INVALIDATED via WTNS path; cycle 37 investigates AV-encoder rejection); count=1 reserved1=3..4 + stripe(s) visible → G3 (`main()` entered AND paint(0) ran; apply cycle-32 F-row rules for in-`main()` half); G4 = G3 + XCTR A1/A2 success shape (full success across both mechanisms). **Codex validation per rule #15** completed (non-trivial diff > 30 LOC in apple-silicon scripts); validation marker written at `.claude/state/codex-validate-last-run` on LOOKS GOOD. **Cycle 35 does NOT include a real-Xbox run** — the bounded assignment was "ship the option-(1) infrastructure so Hermes can later schedule cycle 36 from durable docs." **Cycle 36 (Hermes's call):** FTP-deploy cycle-35 `witness-only/bin/default.xbe` (155 648 B; SAME path `/E/Apps/witness-only/default.xbe`; the FTP uploader's default size-only diff would otherwise skip the upload because the size matches cycle 31's exactly — MUST pass `--overwrite`; cycle-30 methodology lesson) + ARM composite-capture leg via `composite-record.sh cycle36-witness-only-pre-main` + runxbe + final `witness.scan-self` to read the `reserved1` counter for G-row classification. ZERO host xemu source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `lib/lib.mk` touched; ZERO `oracle-agent/*` touched (cycle-27 preserve gate + cycle-29 `witness.scan-self` verb intact); ZERO `xbed_runtime.{c,h}` touched; ZERO image-blit touched; ZERO `nxdk/` source touched (`.CRT$X*` mechanism CONSUMED from stable nxdk CRT API, NOT modified); ZERO `tools/xemu-capture/` source touched; ZERO `composite-record.sh` / `composite-preflight.sh` source touched (cycle-33 implementations intact; cycle-34 secondary findings remain filed for separate cycle). Twelve pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the rolling Hermes-supervision pattern; pre-existing tracked drift in `capture-composite-reference.sh` + 3 `retail-*.py` scripts AND pre-existing untracked `composite_preflight.py` PRESERVED unstaged per cycle-34 prompt guardrail (carried forward). M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-36 real-Xbox discriminator run + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED.

- **Cycle 34 (CLOSED 2026-05-23; closure commit `b5327d4d17`):** Cycle-32 redo on real Xbox vs cycle-31 visual-breadcrumb build — **OUTCOME F4** per cycle-31 cycle-32 9-row discriminator table (`scripts/apple-silicon/xbe-tests/witness-only/README.md` rows F1..F8 + F4'). The cycle-32 redo finally moved from F8 (no-capture-procedural-failure recorded at cycle-32 closure `ac515383bb`) to F4 (genuine discriminator answer) now that Josh's physical Xbox restart + Mac Studio QuickTime composite-capture verification has cleared the cycle-32 / cycle-33 hardware-side blocker. Reads as `(deepest visible stripe = none, witness.scan-self count = 0, witness.scan = D-cycle-27)`: zero stripes visible across 22 NTSC-correct composite snapshots over t+0.07s..t+24.17s after the second `runxbe witness-only` (the cycle-31 final `Sleep(2000)` design holds the deepest stable paint state for ~60 frames at 30 fps which is well above the burst's 1.2 s sampling cadence — a single-stripe-only landing or any-band-of-color landing should have shown); final `witness.scan = count=1 phys=0x03eb3000 reserved0=0 reserved1=0 mapped_pages_seen=419` AND final `witness.scan-self = count=0` (matches cycle-30 / cycle-32 post-states exactly). Only F4 matches all three axes of the 9-row table simultaneously: F1/F2/F3/F5/F6 require >=1 visible stripe (DOES NOT MATCH); F4' requires `witness.scan-self count>=1` from the cycle-29 self-witness fires running after a graceful `XVideoSetMode` FALSE return latches `xbed_breadcrumb_init` to FAILED while `main()` continues past paint(0) (the observed `count=0` REFUTES the continuation path and therefore eliminates F4'); F7 requires partial intermediate band-gap shape (not observed); F8 ("no composite capture available") DOES NOT APPLY because the capture leg ran end-to-end and the pre-runxbe dashboard snap_00 in `snapshots-runxbe2-ntsc/` (25 357 unique colors, max=(255,255,255)) proves the capture pipeline healthy. F4 maps to **(γ.0)** "execution never entered `main()` AT ALL" OR **(γ.1)** "`XVideoSetMode` itself faulted hard before returning" per the cycle-31 cycle-32 discriminator table row 4. Hypothesis state: (γ) "main() never reaches the fire calls" promoted from LEADING (cycle 30 E2 / cycle 32 F8 inconclusive on stripes) to **LEADING at STRONGEST FORM**; cycle-22 pre-main-crash hypothesis promoted from RE-STRENGTHENED-toward-leading-but-not-fully-corroborated to **FULLY CORROBORATED in its strongest form**; (α) "kseg0 scan can't find XCTR from non-agent context" + (β) "scan finds XCTR but write faults silently" STILL LIVE but FURTHER DEPRIORITIZED (any successor design must still account for them, but neither is the LEADING cause); hypothesis #5 catastrophic-hang sense UNCHANGED PARTIALLY INVALIDATED (cycle 34 reproduced clean dashboard recovery, not a 928 s+ hang). Sequence executed (9 logged steps under `benchmark-runs/cycle34-cycle32-redo-real-xbox-witness-only-visual-20260523T203702Z/`, gitignored per project convention): 00 reachability + composite preflight (`status=ok` 1.671 s via xemu-capture; real 720x480 NTSC dashboard probe); 01 ftp-list confirms cycle-31 witness-only (155 648 B) + cycle-29 oracle-agent (417 792 B) still resident at expected `/E/Apps/...` paths (preserved across Josh's power-cycle since both live on HDD); 02 ensure-agent SITE EXEC OK; 03 baseline both scans MET (matches cycle-30 / cycle-32 baselines exactly); 04 composite-record.sh aborted at preflight gate (`status=no_backend`; ffmpeg unavailable on bash PATH AND silent-stalls when explicitly resolved — TCC inheritance asymmetry); switched to xemu-capture snapshot burst (ZERO source changes; substitutes ffmpeg AVFoundation recording with shell-orchestrated single-frame captures whose cumulative coverage is well above the cycle-31 final-Sleep window); 05 first `runxbe` issued — first burst into `snapshots/` mistakenly used xemu-capture's 720x576 PAL default dimensions because the bare `snapshot DEVICE --out PATH` invocation does NOT pass `--width`/`--height` (all 19 PNGs identical-SHA `ef23436c5368...` pure-zero pixels; PRESERVED for audit and as documentary evidence of the cycle-33 step-11 example-invocation gap); 06 post-first-runxbe Xbox state `ping=true, ftp=true, agent=false` (clean dashboard recovery); 07 post-first-runxbe scans = `(D-cycle-27, count=0)` already enough to land F4 if the no-stripe finding holds after the format mismatch is corrected; 08 second `runxbe` issued with `--width 720 --height 480` explicit in every snapshot invocation — pre-runxbe snap_00 in `snapshots-runxbe2-ntsc/` shows real dashboard signal (25 357 unique colors) AND 22 post-runxbe snaps over t+0.07s..t+24.17s ALL pure black (unique=1); 09 final post-burst-15s ensure-agent + scans = `(D-cycle-27, count=0)` matching cycle-32 exactly. Reproducibility wins: kernel-pool deterministic `phys=0x03eb3000` REPRODUCED across cycle-34 baseline + post-first-runxbe + post-second-runxbe = 3 readbacks this session; combined with prior cycles **≥18 consecutive observations across cycles 26 / 28 / 30 / 32 / 34** now across TWO physical power sessions (cycle 34 is the fresh session post Josh's physical restart — confirms the kernel pool's reuse pattern survives across physical power cycles, stronger than prior single-power-session claim); `mapped_pages_seen=419` REPRODUCED at every readback this session (matches cycles 28 / 30 / 32 exactly). Secondary findings filed for cycle-35+ consideration (NOT cycle-34 scope): (i) **cycle-33 preflight `--mode auto` does not protect `composite-record.sh` (ffmpeg-based) from the xemu-capture-yes / ffmpeg-no TCC asymmetry** exposed this cycle — `ffmpeg -frames:v 1 -f avfoundation` from this bash session silent-stalls (ZERO bytes of stderr; reproduces cycle-32 OUTCOME F8 shape locally) while xemu-capture against the same device succeeds in <2 s (hypothesized TCC camera-access permission inheritance asymmetry); cycle-35+ could add `--require-both-detectors` OR have `composite-record.sh` always run an ffmpeg liveness check regardless of xemu-capture outcome; (ii) **xemu-capture snapshot defaults to 720x576 PAL** which decodes the NTSC composite signal as pure-zero RGB(0,0,0) while still reporting `status=ok` — the `witness-only/README.md` line-180 step-11 example invocation does NOT pass `--width`/`--height` and could mislead an operator into a false F4-shape reading; cycle-35+ could either default xemu-capture snapshot dimensions to NTSC for MS2109 source OR update the example invocation in README.md to include dimensions. ZERO source/script code edits this cycle; ZERO XBE rebuilds; ZERO host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `oracle-agent/*` touched (cycle-27 preserve gate + cycle-29 `witness.scan-self` verb intact); ZERO `xbed_runtime.{c,h}` touched; ZERO image-blit touched; ZERO `witness-only/main.c` touched (cycle-31 source intact); ZERO `tools/xemu-capture/` source touched; ZERO `scripts/apple-silicon/composite-record.sh` source touched (cycle-33 implementation intact); ZERO `scripts/apple-silicon/composite-preflight.sh` source touched (cycle-33 implementation intact). **Codex SKIPPED** under rule #15 doc-only / run-only carve-out (same path as cycles 26 / 28 / 30 / 32); the cycle-31 binary observed this cycle is bit-identical to the cycle-31 build that passed 3-round Codex green at cycle-31 closure (`41f350c174`); cycle-31 marker at `.claude/state/codex-validate-last-run` remains the relevant marker for the deployed artifact. Pre-existing tracked-but-uncommitted modifications to `scripts/apple-silicon/capture-composite-reference.sh` + `retail-gameplay-oracle.py` + `retail-oracle-workflow.py` + `retail-title-automation-proof.py` PRESERVED unstaged per the cycle-34 prompt guardrail; pre-existing untracked `scripts/apple-silicon/composite_preflight.py` PRESERVED unstaged; 11 pre-existing untracked `.hermes_*.txt` + `.hermes_launch_*.sh` files at repo root preserved un-staged per the rolling Hermes-supervision pattern. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-35+ pre-main breadcrumb implementation + downstream cycles), §G.5, RT-as-texture; `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED; cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated; flag continues to ship opt-in, default OFF. Cycle-35+ pre-main breadcrumb design is Hermes's call: candidates per the cycle-31 cycle-32 discriminator table F4 "Next" column include (1) nxdk `.CRT$XCU` static-init slot stamp that runs after PE-load but before `main()`; (2) custom XBE-header callback (kernel-controlled entry slot, runs before `.CRT$*`); (3) thinner alternative to `XVideoSetMode` (e.g. direct NV2A CRTC register writes that bypass the kernel display init path).

- **Cycle 33 (CLOSED 2026-05-23; closure commit `dcaf7a0206`; bounded closeout-sync follow-up commit on top syncs orchestration-state quartet):** Composite-capture fail-fast preflight slice. Shipped new `scripts/apple-silicon/composite-preflight.sh` (~310 lines; xemu-capture snapshot primary detector + ffmpeg `-frames:v 1` fallback against the resolved AVFoundation numeric index + JSON output schema `composite-preflight/v1` + 5-line physical-side checklist on any non-zero exit; exit codes 0 ok / 2 no_signal / 3 device_not_found / 4 no_backend / 5 invalid CLI / 1 unexpected) + default-on integration into `scripts/apple-silicon/composite-record.sh` (new flags `--skip-preflight`, `--preflight-timeout SECONDS` (default 8), `--preflight-mode auto|xemu-capture|ffmpeg`; env-var overrides `COMPOSITE_PREFLIGHT_TIMEOUT` + `COMPOSITE_PREFLIGHT_MODE`; on preflight failure writes stub `capture-meta.json` schema `composite-record/v1` with `status="preflight-failed"`, `ffmpeg_invoked=false`, embedded `preflight` summary, and exits the preflight's own rc WITHOUT touching ffmpeg; on success the post-run capture-meta.json is patched with the same `preflight` summary alongside existing ffmpeg_rc / wall_elapsed_s / video_duration_s fields). Local validation: live no-signal MS2109 probe rc=2 in ~3.6 s (vs cycle-32's ~93 s = >25× speedup at unattended-orchestration boundary); end-to-end `composite-record.sh` preflight-failure produces structured `preflight-failed` capture-meta.json with ffmpeg never launched; `--skip-preflight` reproduces cycle-32 silent-stall behavior unchanged (escape hatch preserved); `bash -n` clean for both scripts. Paired doc updates: `docs/apple-silicon/automation.md` (new "Composite capture preflight" section + 3 new flag rows + new "Preflight default (cycle 33)" paragraph in the composite-record section), `.claude/rules/flags-bench.md` (new "Composite-capture preflight (cycle 33)" subsection), plus `handoff.md` + `decision-log.md` cycle-33 entries on top. Scope contained strictly inside composite tooling + directly paired docs/rules/state: ZERO host renderer / TCG / NV2A / build changes; ZERO XBE rebuilds; ZERO `lib/xbed_*` / `oracle-agent/*` / `witness-only/main.c` / image-blit / `xbed_runtime.{c,h}` touched; ZERO `tools/xemu-capture/` source touched; ZERO real-Xbox run; ZERO cycle-32 redo. Rule #15 trigger #2 fires (non-trivial apple-silicon script diff > 30 lines); Codex validation closed at 6 rounds at cycle-33 closure (rounds 1-5 MINOR ISSUES, all adopted/deflected; round 6 LOOKS GOOD); validation marker written at `.claude/state/codex-validate-last-run`; closure commit landed as `dcaf7a0206`. Bounded closeout-sync follow-up commit on top is doc-only / state-only (orchestration-state quartet updated to reference the landed hash; no source/script touched); rule #15 doc-only / state-only carve-out applies to the closeout-sync slice itself — no Codex required. 9 pre-existing untracked `.hermes_*` files at repo root preserved un-staged per the rolling Hermes-supervision pattern. Cycle-32 redo remains Hermes's call after physical-side composite-cable / capture-input verification; cycle-33 preflight makes that redo safer to run unattended because step 8 of the `witness-only/README.md` cycle-31 addendum §"Cycle-32 deployment runbook" (`composite-record.sh ARM`) will now refuse to proceed if the cycle-32 physical-side problem is still present.

- **Cycle 32 (CLOSED 2026-05-23; closure commit `ac515383bb`):** Path A.4 real-Xbox deployment of the cycle-31 visual-breadcrumb build vs the cycle-31 witness-only XBE. **OUTCOME F8** per `witness-only/README.md` cycle-31 addendum cycle-32 discriminator table (F8 = "no composite capture available — capture leg not running / capture-card not detected. Cycle-32 procedural failure, NOT a discriminator answer. → Cycle 32 redo with composite capture confirmed armed."). Xbox-side leg completed cleanly: cycle-29 oracle-agent resident from cycle 30 (`ping=true, ftp=false, agent=true` at session start); baseline `witness.scan = D-cycle-27 (count=1 phys=0x03eb3000 reserved0=0 reserved1=0 mapped_pages_seen=419)` AND baseline `witness.scan-self = count=0` (preconditions MET, matching cycle 28/30 baselines exactly); reboot → dashboard FTP `226` at t+12 s (authenticated `curl -u xbox:xbox`); `--overwrite` FTP-upload of cycle-31 `witness-only/bin/default.xbe` (155 648 B; SHA-256 `c00c726c96f2172badbe0dcd20c111ab89eee95960b8ce43d03c472db4e09edb`) to `/E/Apps/witness-only/default.xbe`; post-upload list confirms `155648 Dec 10 17:17`; ensure-agent OK; pre-run scans still MET; `runxbe 'E:\Apps\witness-only\default.xbe'` at `2026-05-23T13:59:46Z`; dashboard `226` at t+30 s after runxbe (9 s FASTER than cycle 30's t+39 s — weak single-sample signal, NOT load-bearing; mildly inconsistent with full-success interpretation given cycle-31 added ~2 s of expected work, mildly consistent with early-crash interpretation); post-run `ensure-agent` + final scans = `witness.scan = D-cycle-27` AND `witness.scan-self = count=0` (functionally IDENTICAL to cycle 30's E2). Composite-capture leg FAILED at hardware level: AVFoundation enumerates `AV TO USB2.0` at video=[0] + audio=[3]; `composite-record.sh --duration 70 --label cycle32-witness-only-screen` launched ffmpeg cleanly (substring resolver matched both video and audio MS2109 interfaces), but ffmpeg produced ZERO bytes of stderr + ZERO video.mp4 across 70 s of `-t` plus the 20 s composite-record watchdog grace; SIGKILL at wall-elapsed 93 s (`rc=137`, `capture_timed_out=true`). Follow-up 4 s standalone ffmpeg probe against the same device (no audio mux, no encoder choice difference; `ffmpeg ... -i 0:none -t 4 ...`) reproduced the same silent-no-frames behavior across 60+ s before manual SIGKILL — confirms hardware-side capture failure (no live composite signal at MS2109 input), not a cycle-32 procedural bug in `composite-record.sh`. Most likely physical-side causes (cannot be remediated from a Claude session): composite cable not seated at Xbox AV port; MS2109 input selector on S-Video instead of composite; Xbox AV output not on composite. Per the cycle-31 cycle-32 discriminator table, **F8 is the canonical name for this shape**; classifying as one of F2/F3/F4/F5 would over-claim because those four rows share the cycle-32 two-tuple `(D-cycle-27, count=0)` and are distinguishable ONLY by the visible-stripe count, which is missing. **F4' (graceful XVideoSetMode FALSE return) RULED OUT** by cycle-32's `witness.scan-self count=0` readback (F4' requires count >= 1); eliminated from cycle-32-redo candidate space. F6 / E1 / E1' / E1''-shaped outcomes also RULED OUT. Hypothesis state UNCHANGED from cycle 30: (γ) "main() never reaches the fire calls" STILL LEADING; (α) STILL LIVE but DEPRIORITIZED; (β) STILL LIVE but DEPRIORITIZED; cycle-22 pre-main-crash hypothesis STILL RE-STRENGTHENED toward leading but NOT fully corroborated (cycle-32 redo with composite capture confirmed armed will move it: F4 → fully corroborated in strongest form; F1/F2/F3/F5 → strong INVALIDATION via stripe 0 visible). Reproducibility wins this cycle: kernel-pool deterministic `phys=0x03eb3000` reuse REPRODUCED across baseline + pre-run + post-run readbacks = **≥12 consecutive observations across cycles 26 / 28 / 30 / 32** in same physical power session; `mapped_pages_seen=419` REPRODUCED at 8th-10th observations. ZERO source/script code edits this cycle; ZERO XBE rebuilds; ZERO host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched (cycle-29 self-witness shim intact); ZERO `oracle-agent/*` touched (cycle-27 preserve gate + cycle-29 `witness.scan-self` verb intact); ZERO `xbed_runtime.{c,h}` touched; ZERO image-blit touched. Codex SKIPPED under rule #15 doc-only / run-only carve-out (same path as cycles 26 + 28 + 30); the cycle-31 binary deployed this cycle is bit-identical to the cycle-31 build that passed 3-round Codex green at cycle-31 closure. Evidence preserved on disk: `benchmark-runs/cycle32-real-xbox-witness-only-visual-20260523T135646Z/{00..12-*.log, SUMMARY.md, composite-cycle32/{capture-meta.json, capture-stderr.log}}` (gitignored per project convention; `composite-cycle32/video.mp4` was never produced). Five pre-existing untracked `.hermes_cycle22_path_a3_prompt.txt` + `.hermes_cycle23_docsync_prompt.txt` + `.hermes_cycle31_option_d_prompt.txt` + `.hermes_cycle31_closeout_prompt.txt` + `.hermes_cycle32_real_xbox_prompt.txt` (plus `.hermes_launch_cycle31_closeout.sh` + `.hermes_launch_cycle32.sh`) at repo root preserved un-staged per the rolling Hermes-supervision pattern. **Cycle 32 redo (Hermes's call):** (1) hardware-side verify composite cable seated at Xbox AV port + verify MS2109 input selector is composite (not S-Video); optionally build `tools/xemu-capture/` and use `xemu-capture snapshot USB2 --out /tmp/probe.png` to prove live frames are arriving (the only xemu-capture verb that actually proves frame delivery — `probe` only lists supported formats and `inputs` only enumerates source selector positions; use `inputs` + `set-input` ONLY AFTER snapshot succeeds to confirm the active input is composite vs S-Video — cycle-33 closeout Codex round-1 low #4 adopted to keep the preserved forward-looking redo guidance aligned with the new cycle-33 canonical guidance); (2) capture smoke test (`composite-record.sh --duration 4 --label smoke` should produce a non-empty video.mp4; visually inspect first frame for an Xbox-dashboard frame); (3) OPTIONAL power-cycle Xbox; (4) re-run the canonical cycle-32 sequence per `witness-only/README.md` cycle-31 addendum §"Cycle-32 deployment runbook" steps 1-11 verbatim. On redo, F-row landings collapse to F1 / F2 / F3 / F4 / F5 / F6 / F7 (F4' eliminated). `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED; cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated; flag continues to ship opt-in, default OFF. M15 overall still NOT MET pending §H.6 default-on shape (now blocked on cycle-32 redo with composite capture confirmed armed + downstream cycle 33 work), §G.5, RT-as-texture.

- **Cycle 31 (CLOSED 2026-05-23; closure commit `41f350c174`):** Path A.4 option (d) on-screen visual breadcrumb — IMPLEMENTATION-ONLY bounded code slice; ZERO real-Xbox run; cycle-32 real-Xbox deployment is Hermes's call. Modified `scripts/apple-silicon/xbe-tests/witness-only/main.c` to add a pbkit-free `XVideoSetMode(640, 480, 32, REFRESH_DEFAULT)` + 5 horizontal-stripe ARGB8888 paint sites (RED / ORANGE / YELLOW / GREEN / BLUE) interleaved between cycle-25/29 checkpoints in `main()`: paint(0) BEFORE the cycle-25 host-log line (FIRST observable side effect of `main()`); paint(1) after cycle-23 fire1 return; paint(2) after cycle-23 fire2 return; paint(3) after cycle-29 self-fire1 return; paint(4) after cycle-29 self-fire2 return. Pre-reboot Sleep extended from cycle 25's 500 ms to 2 000 ms for composite-capture redundancy at ~30 fps (≥60 frames of deepest-painted state). Helpers (`xbed_breadcrumb_init` 3-state machine UNTRIED / OK / FAILED — latches FAILED on graceful `XVideoSetMode` FALSE return so `paint(1..4)` cannot re-enter the kernel display init path — Codex round-1 high finding #3 adopted; + `xbed_breadcrumb_paint(stage)` 96-row band fill + XVideoFlushFB) live entirely in `witness-only/main.c` — NO shared-lib changes; NO oracle-agent changes; NO image-blit changes. Single new include is `<hal/video.h>` (+`<string.h>` for `memset`). Build: `witness-only/bin/default.xbe` 155 648 B (+4 096 B from cycle 29's 151 552 B); `witness-only.iso` 720 896 B (unchanged — same ISO sector boundary). Codex 3 rounds: round 1 = MAJOR ISSUES with 3 high + 1 low findings (HIGH #1 canonical docs not synced; HIGH #2 F1 row contradictory — said "both mechanisms landed" when `witness.scan` D-cycle-27 means XCTR did NOT land; HIGH #3 init retried `XVideoSetMode` after failure, broadening risk surface beyond "single call"; LOW #4 banner header stale), all 4 adopted; round 2 = MAJOR ISSUES with 1 new HIGH + 1 new LOW (all round-1 findings RESOLVED): HIGH #5 F4 self-contradictory — bundled graceful XVideoSetMode FALSE return with "cycle-22 pre-main FULLY CORROBORATED" though a graceful FALSE means main() DID execute past its first instruction; LOW #6 stripe-map prose stale ("All 5 stripes = E1 shape" no longer accurate with F1/F5/F6 split). Both adopted: F4 split into F4 (γ.0 / γ.1 only) + new F4' (graceful FALSE = γ INVALIDATED via WTNS path); stripe-map prose rewritten; discriminator-table row count went from 8 to 9. Round 3 = LOOKS GOOD with no new findings; validation marker written. **Cycle 31 does NOT include a real-Xbox run.** Cycle 32 (Hermes-scheduled) will FTP-deploy the cycle-31 `witness-only/bin/default.xbe` (same path `/E/Apps/witness-only/default.xbe`; cycle-29 oracle-agent stays in place from cycle 30) and run the cycle-30 canonical sequence WITH the composite-capture leg ARMED via `scripts/apple-silicon/composite-record.sh` BEFORE issuing `runxbe`. Expected outcomes F1..F8 enumerated in `witness-only/README.md` cycle-31 addendum + `manifest.json` `expected_results.real-xbox/physical/cycle-32`: F1 (all 5 stripes + WTNS success + XCTR D-cycle-27) — γ INVALIDATED; α/β remain live on XCTR side → cycle 33 option (b); F3 (stripe 0 only + count=0) — strong cycle-23 mechanism failure → redesign; F4 (no stripes + count=0) — γ.0 OR γ.1 — cycle-22 pre-main hypothesis FULLY CORROBORATED → cycle 33 pre-main breadcrumbs; F4' (no stripes + WTNS count=1) — graceful XVideoSetMode FALSE return — `main()` DID execute → γ INVALIDATED via WTNS path (Codex round-2 high finding adopted: F4' was originally collapsed into F4 — a graceful FALSE return is logically distinct from γ.0/γ.1 because the cycle-29 self-witness fires still execute and stamp the WTNS page). ZERO xemu-fork host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched; ZERO `lib/xbed_self_witness.{c,h}` touched; ZERO `oracle-agent/*` touched; ZERO `xbed_runtime.{c,h}` touched; ZERO image-blit touched. Three pre-existing untracked `.hermes_cycle22_path_a3_prompt.txt` + `.hermes_cycle23_docsync_prompt.txt` + `.hermes_cycle31_option_d_prompt.txt` at repo root preserved un-staged per the rolling Hermes-supervision pattern.

- **Cycle 30 (CLOSED 2026-05-23; closure commit `dfe1480cba`):** Path A.4 real-Xbox deployment of the cycle-29 self-allocated-witness build vs cycle-29 witness-only XBE (formerly "this session" before cycle 31; preserved verbatim below). **OUTCOME E2** per `witness-only/README.md` cycle-30 discriminator table: final `witness.scan = D-cycle-27 (count=1 phys=0x03eb3000 reserved0=0 reserved1=0)` (identical to cycle 28) AND final `witness.scan-self = count=0` (no `'WTNS'` page allocated/found anywhere in scanned kseg0). **(γ) "main() never reaches the fire calls" is LEADING**; the cycle-22 pre-main-crash hypothesis is **RE-STRENGTHENED** from weakened toward leading; (α) "kseg0 scan can't find XCTR from non-agent context" and (β) "scan finds XCTR but write faults silently" both REMAIN LIVE but DEPRIORITIZED (moot given γ leading). Because the cycle-29 self-witness fires execute AFTER the cycle-23 XCTR fires (Codex round-1 high finding #1 ordering) AND have no XCTR-side dependency (they call `MmAllocateContiguousMemoryEx` directly with parameters bit-identical to the agent's known-good `s_allocate_fresh`, proven by ≥9 consecutive observations of phys=0x03eb3000 deterministic reuse this session), E2 conclusively rules out "main() ran and fired silently no-op." Hard preconditions MET at every gate: baseline `witness.scan count=1 reserved0=0 reserved1=0` AND baseline `witness.scan-self count=0` (precondition check #1 against cycle-27 resident agent; check #2 against cycle-29 agent after `ensure-agent`). New timing observation: dashboard FTP recovery at **t+39s** (a NEW shape vs cycle 26's 70.17 s + cycle 28's 70 s reproduced witness-only shape; sits between mirror control's ~36 s and the 70 s prior shape; possible readings include faster early crash bypassing the witness lib `.text`, normal variance, or shifted crash site from +4 096 B of linked-in `xbed_self_witness.c` `.text` — tracked open question, single-sample, NOT load-bearing for the E2 conclusion). Cycle-31 leading candidate is **option (d)** on-screen visual breadcrumb (composite capture during witness-only execution OR re-architect witness-only to emit a synchronous pbkit-free framebuffer marker); option (b) (agent-side prior-phys dump + read-only kseg0 dump verb) is DEMOTED because α-vs-β is moot now. ZERO source/script code edits this cycle; ZERO XBE rebuilds; ZERO host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `lib/xbed_self_witness.{c,h}` touched; ZERO `oracle-agent/controller.c` touched (cycle-27 preserve gate intact). Codex SKIPPED under rule #15 doc-only / run-only carve-out (same path as cycles 26 + 28). Methodology lessons encoded for future cycles: (1) authenticated `curl -u xbox:xbox ftp://...` returning `226` is the new ground-truth dashboard-ready probe (cycle-26/28 anonymous probes scored `530 login-required` as CLOSED, undercounting recovery timing); (2) `xbox-ftp-upload.py`'s default size-only diff cannot distinguish cycle-27 vs cycle-29 oracle-agent binaries — future same-size redeploys MUST pass `--overwrite`. Reproducibility wins: kernel-pool deterministic phys=0x03eb3000 reuse REPRODUCED across cycle-27 agent re-launch + cycle-29 first launch + cycle-29 post-chainload launch = ≥9 consecutive observations across cycles 26 / 28 / 30 in same physical power session; `mapped_pages_seen=419` REPRODUCED at 5th-7th observations. Evidence preserved on disk: `benchmark-runs/cycle30-real-xbox-witness-only-self-20260523T103624Z/{00..09-*.log, SUMMARY.md}` (gitignored per project convention). Two pre-existing untracked `.hermes_cycle*.txt` prompt files at repo root preserved un-staged (consistent with cycles 26 / 27 / 28 / 29 handling).

- **Cycle 29 (CLOSED 2026-05-23; closure commit `725bc97bbd`):** Path A.4 option (c) self-allocated witness infrastructure SHIPPED. New shared diag-XBE lib `scripts/apple-silicon/xbe-tests/lib/xbed_self_witness.{h,c}` (`MmAllocateContiguousMemoryEx` + `MmPersistContiguousMemory` self-owned page with unique magic `'WTNS'` = 0x534E5457 + version 1 + reserved0=(0xA4<<24)|stage + reserved1=counter). New oracle-agent read-only verb `witness.scan-self` (same kseg0 [0x80010000, 0x84000000] scan range + 4 KiB stride + `MmGetPhysicalAddress` per-page safety gate as `cmd_witness_scan`, gated to 'WTNS' magic). `witness-only/main.c` calls `xbed_self_witness_fire(MAIN_ENTERED)` + `xbed_self_witness_fire(POST_MARKER0)` AFTER the existing cycle-23 XCTR fires (Codex round-1 high finding #1: ordering keeps cycle-23 path bit-identical to cycle 25 through second cycle-23 fire, so cycle-30 XCTR readback is properly comparable to cycle 28's D-cycle-27). Opt-in via `witness-only/Makefile` only (Codex round-1 low finding #3 — NOT in `lib.mk` default SRCS, so the rest of the diag-XBE corpus is unaffected). Discriminator scope (Codex round-1 high finding #2): cycle 29 is positioned narrowly as a **(γ)-only** discriminator — a `witness.scan-self` hit INVALIDATES (γ) "main() never reached" but (α) "agent buffer not findable from non-agent context" AND (β) "scan finds it but write faults silently" BOTH REMAIN LIVE because the cycle-29 self-witness stamps a self-owned page, not the agent's XCTR page. α-vs-β requires cycle 31+ option (b). Build: `oracle-agent/bin/default.xbe` 417 792 B (size unchanged from cycle 28); `witness-only/bin/default.xbe` 151 552 B (+4 096 B from cycle 25). Codex 4 rounds: round 1 MAJOR ISSUES (3 findings, all adopted), round 2 MINOR ISSUES (2 LOW, both adopted), round 3 MINOR ISSUES (1 LOW, adopted), round 4 LOOKS GOOD; validation marker written. ZERO xemu-fork host source touched; ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact); ZERO `oracle-agent/controller.c` touched (cycle-27 preserve gate intact); ZERO real-Xbox run (cycle-30 scope, Hermes's call).

- Cycle 17 closed the local renderer-agnostic milestone; cycle 18 packaged the state in a doc-only checkpoint.
- Cycle 19 attempted a real-Xbox parity check via the cycle-15 v0.4 image-blit XBE; reproducibly produced zero `D:\image-blit-capture.bin` / `D:\image-blit-done.txt` across two runs. `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on decision DEFERRED.
- Cycle 20 took Path A: 13 staged `D:\image-blit-marker-NN-STAGE.txt` markers; local xemu validation showed all 13 fire; every fopen returned NULL on the ISO mount; two real-Xbox runs BOTH produced zero marker files; chainload→FTP-back stable at 22.4 s.
- Cycle 21 took Path A.2: re-route markers from `D:\` to `E:\Apps\image-blit\…`. Local validation 4 boots OK; single real-Xbox run produced clean NEGATIVE — 4th independent 22.4 s reproduction, `verdict.json status: ok`, ZERO marker files. Cycle-19 hypothesis #1 demoted from "leading" to "insufficient as sole explanation."
- Cycle 22 took Path A.3: provenance audit of `docs/apple-silicon/xbox-real-references/*` captures. All five reference sets WERE produced through the same `XLaunchXBE` chainload mechanism (HIGH confidence). Cycle-19 hypothesis #1 fully INVALIDATED. Image-blit failure re-classified as **image-blit-specific**. Cycle-22 leading hypothesis: image-blit crashes BEFORE main()'s first instruction.
- Cycle 23 took Path A.4: shipped non-fopen kernel-pool controller-buffer witness for image-blit + agent-side `witness.scan` RPC. Local xemu-Metal validation green. Codex round-1 BLOCK → all 4 findings adopted → round-2 PASS_WITH_FINDINGS → MINOR PARTIAL closed post-round-2. Closure commit `5fce3b14e4`; doc-sync follow-up `b614bdbc83`.
- Cycle 24 ran the cycle-23 witness on real Xbox. CLOSED with a CONCRETE BLOCKER. Cycle-23 binaries deployed via FTP. Baseline `witness.scan` precondition MET. `runxbe E:\Apps\image-blit\default.xbe` issued. Xbox went fully silent for 928.3 s before measurement was aborted. Post-chainload `witness.scan` UNRECOVERABLE without a physical power-cycle that erases the persistent buffer. Cycle-22 leading hypothesis ("pre-main crash") WEAKENED. NEW hypothesis #5: kseg0-scan witness mechanism may be real-Xbox-unsafe from a non-agent process context. Closure commit `487e729d4f`; doc-sync follow-up `29a455a78e`.
- Cycle 25 implemented the cycle-24-recommended witness-only diagnostic XBE. SHIPPED + CLOSED. 5 new files under `scripts/apple-silicon/xbe-tests/witness-only/` + built bin/default.xbe (147 456 B) + witness-only.iso (720 896 B). Codex 3-round green.
- Cycle 26 ran the cycle-25 witness-only XBE on real Xbox. CLOSED — outcome shape D. ~70.17 s chainload→dashboard-ready vs 20.67 s invalid-path control vs ~36 s mirror control. Post-run `witness.scan` after EVERY chainload identical to baseline; NO orphan observed across 4 chainloads. Hypothesis #5 PARTIALLY INVALIDATED in the catastrophic-hang sense. Stamp-vs-no-stamp ambiguity OPEN. Cycle-22 leading hypothesis status: still WEAKENED. Closure commit `a31e061144`.
- **Cycle 28 ran the cycle-27 preserve-branch oracle-agent on real Xbox against the cycle-25 witness-only XBE:** outcome **D-cycle-27** confirmed (`count=1 live=1 reserved0=0x00000000 reserved1=0x00000000` in final `witness.scan` after cycle-27 agent re-allocates the deterministic kernel-pool buffer phys=0x03eb3000). The cycle-27 preserve gate's strict predicate `(reserved0==0, reserved1==0)` OR `((reserved0>>24)==0xA4, 1<=reserved1<=4096)` covers both header shapes `xbed_a4_witness.c` actually writes (MAIN_ENTERED → reserved0=0xA4000001 reserved1=1; POST_MARKER0 → reserved0=0xA4000003 reserved1=2); any landed stamp would have survived the preserve branch and shown up in readback. The observed `(0,0)` unambiguously means **no A.4-tagged stamp existed on the page at agent re-allocation time** = stamp never landed. The cycle-26 "stamp landed and got wiped" hypothesis is therefore **INVALIDATED**; cycle-27 option (a) is **demonstrated insufficient** for breaking cycle-26 ambiguity to A1/A2. Sequence executed: reachability probe → baseline `witness.scan` (precondition MET; matches cycle-26 exactly) → `reboot` (dashboard FTP-LIST at t+27s) → FTP-upload cycle-27 oracle-agent (417 792 B; verified) → confirm cycle-25 witness-only still resident (147 456 B) → `ensure-agent` launches cycle-27 build (banner unchanged, expected) → post-launch rescan identical to baseline → `runxbe witness-only` (dashboard fully ready at t+70s, matches cycle 26's 70.17 s) → post-run `ensure-agent` + final `witness.scan` = D-cycle-27. Reproducibility wins: 70 s dashboard-recovery shape REPRODUCED across cycle 26 + cycle 28 (real Xbox behavior, not a one-off); kernel-pool deterministic phys=0x03eb3000 reuse REPRODUCED (≥6 consecutive observations in same physical power session); `mapped_pages_seen=419` REPRODUCED. Hypothesis #5 status: catastrophic-hang sense remains PARTIALLY INVALIDATED (cycle 28 reproduces 70 s recovery, not 928 s+ hang); subtler "silently no-ops" sense now consistent with cycle-28 evidence but indistinguishable from (γ) "witness-only never reaches main()." Cycle-22 leading hypothesis: still WEAKENED (cycle-28 evidence equally consistent with "main() runs and fires no-op silently" and "main() never reached"). ZERO source/script code edits, ZERO XBE rebuilds; run-only / doc-only slice; Codex skipped under rule #15 carve-out (same path as cycle 26). Evidence preserved on disk under `benchmark-runs/cycle28-real-xbox-witness-only-preserve-20260523T084331Z/` (9 logs + SUMMARY.md; gitignored per project convention). Two pre-existing untracked `.hermes_cycle*.txt` prompt files at repo root NOT staged (consistent with cycle 26 + cycle 27 handling).

- **Cycle 27 implemented option (a) from the cycle-26 closure's four candidate cycle-27 designs:** `oracle-agent/controller.c::s_allocate_fresh` now PRESERVES an existing plausible `oracle_ctrl_buffer` witness header across agent restart instead of unconditionally `memset`-wiping it. New static helper `s_page_has_plausible_witness_header(vp)` with predicate TIGHTENED to a strict subset of the cycle-23 scan filter (Codex round-1 medium): accepts only `(reserved0==0, reserved1==0)` or `((reserved0>>24)==0xA4, 1<=reserved1<=4096)`. Preserve branch clears only `port[]`; legacy branch keeps full-zero + re-stamp magic/version. One conditional `debugPrint` breadcrumb when preserve fires. Paired doc edits in `controller.h`, `commands.c::cmd_witness_scan` body comment, `witness-only/README.md` discriminator table, and `witness-only/manifest.json` add the new positive success shape `count=1 live=1 reserved0=0xA4xxxxxx` alongside the legacy orphan shape. oracle-agent XBE rebuilt (417 792 B; size unchanged). Codex 3-round: round 1 = MINOR ISSUES (medium + low; both adopted); round 2 = MINOR ISSUES (round-1 MEDIUM RESOLVED; round-1 LOW PARTIAL adopted); round 3 = LOOKS GOOD. Validation marker written. ZERO host source touched; ZERO image-blit / witness-only / xbed_a4_witness source touched. Two pre-existing untracked `.hermes_*.txt` prompt files at repo root NOT staged.

## Cycle 29 design + outcome (locked at session close 2026-05-23)

| Step | Action | Outcome |
|---|---|---|
| 1 | Read required docs + state files + source | Plan confirmed; cycle-23 lockstep contract + cycle-27 preserve-gate identified as the invariants to preserve |
| 2 | Design unique magic + 16-byte header layout | `'WTNS'` (0x534E5457); symmetric with `oracle_ctrl_buffer` for scanner reuse; counter ceiling 4096 mirrors cycle-23 plausibility filter |
| 3 | Create `lib/xbed_self_witness.{h,c}` + wire into lib.mk + add fires in `witness-only/main.c` + add agent verb `witness.scan-self` | 7 files modified (3 substantive: shim .c, shim .h, witness-only/main.c, oracle-agent/commands.c; 4 supporting: lib.mk, oracle-agent/commands.h, oracle-agent/main.c, witness-only/Makefile) + 2 paired-doc files (witness-only/README.md, witness-only/manifest.json) |
| 4 | Rebuild oracle-agent + witness-only XBEs | `oracle-agent/bin/default.xbe` 417 792 B unchanged; `witness-only/bin/default.xbe` 151 552 B = +4 096 B from cycle 25 |
| 5 | Codex round 1 (changes mode) | MAJOR ISSUES with 3 findings: HIGH #1 ordering confound, HIGH #2 discriminator over-claim, LOW #3 lib.mk over-scope. All 3 adopted |
| 6 | Rewrite ordering + docs + Makefile linkage; rebuild XBEs | Cycle-29 fires moved to AFTER cycle-23 fires; 5 doc surfaces rewritten to (γ)-only positioning; `xbed_self_witness.c` opt-in only via witness-only/Makefile |
| 7 | Codex round 2 | MINOR ISSUES with 2 LOW (round-1 HIGH #1 PARTIAL — header doc residue; new LOW — operator-tables missed partial-success shapes). Both adopted |
| 8 | Expand operator tables to 7 rows; harmonize header doc | Cycle-30 table covers E1/E1'/E1''/E2/E3/E4/E5 in README + manifest |
| 9 | Codex round 3 | MINOR ISSUES with 1 LOW (round-2 LOW PARTIAL — tolerated `0xA4000003 reserved1=1` second-self-fire-only shape uncalled). Adopted |
| 10 | Add "representative not exhaustive" caveat + document reader's acceptance rule | README + manifest now self-describing for novel readback shapes |
| 11 | Codex round 4 (confirmation) | **LOOKS GOOD.** Round-3 LOW RESOLVED. No new findings |
| 12 | Write validation marker | `.claude/state/codex-validate-last-run` updated |
| 13 | Canonical docs sync | handoff.md cycle-29 entry on top with cycle-28 preserved unchanged; decision-log cycle-29 entry above cycle-28; orchestration-state quartet closure pass |
| 14 | Closure commit on `apple-silicon-performance` | `725bc97bbd` |

## Cycle 29 scope discipline

- ZERO xemu-fork host source touched (no `hw/`, `ui/`, `target/`, `include/`).
- ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact across writer + cycle-23 reader + cycle-27 preserve gate).
- ZERO `oracle-agent/controller.c` touched (cycle-27 preserve gate intact; new agent verb is purely additive next to `cmd_witness_scan`).
- ZERO image-blit source touched.
- ZERO retail-title / §G.5 / RT-as-texture / second-wave-XBE work.
- ZERO flag default flips.
- ZERO real-Xbox deployment (cycle-30 scope, Hermes's call).
- ZERO PushNotification — bounded implementation slice, not blocker / milestone.
- `xbed_self_witness.c` INTENTIONALLY NOT in `lib/lib.mk` default SRCS — opt-in only via `witness-only/Makefile` (Codex round-1 low finding #3).
- Two pre-existing untracked `.hermes_cycle22_path_a3_prompt.txt` + `.hermes_cycle23_docsync_prompt.txt` at repo root preserved un-staged per Hermes pre-session instruction.

## Important interpretation notes (cycle 29)

- **Cycle 29 is positioned narrowly as a (γ)-only discriminator.** A cycle-30 `witness.scan-self` hit invalidates (γ) "main() never reaches the fire calls" but leaves (α) "scan can't find XCTR" AND (β) "scan finds XCTR but write faults silently" BOTH LIVE. The cycle-29 self-witness stamps a SELF-OWNED page, not the agent's XCTR page; it does not exercise the failing write into the agent's XCTR buffer. α-vs-β discrimination requires cycle 31+ option (b) (agent-side prior-phys dump + read-only kseg0 dump verb).
- **Cycle 29 is additive but NOT a strict superset of cycle 25.** Through the second cycle-23 fire, cycle 29 is bit-identical to cycle 25 (same instruction sequence, same kernel state on entry). After the second cycle-23 fire, cycle 29 introduces new kernel-allocator activity (one `MmAllocateContiguousMemoryEx` + `MmPersistContiguousMemory` + page wipe + four 32-bit writes per fire). A cycle-22-style pre-main crash that happens BEFORE the cycle-23 fires would still surface as γ (`WTNS count=0`); a crash specifically between the cycle-23 fires and the cycle-29 fires would yield a NEW shape only cycle 29 can produce (`XCTR D-cycle-27` + `WTNS count=0`).
- **Cycle-30 readback table is representative, not exhaustive.** The agent reader accepts any tagged WTNS page with `(reserved0 >> 24) == 0xA4` AND `reserved1` in `[1, 4096]`, plus the canonical fresh-init shape `(0, 0)`. Partial-success edge cases (e.g. `reserved0=0xA4000003 reserved1=1` second-self-fire-only) are tolerated by the reader and should be interpreted as γ-invalidating with the counter + stage byte pinpointing how far main() got.

## Next bounded slice (cycle 30 — Hermes-scheduled)

FTP-deploy cycle-29 `oracle-agent/bin/default.xbe` + cycle-29 `witness-only/bin/default.xbe` to real Xbox. Canonical cycle-26-style sequence extended with `witness.scan-self` queries at baseline + post-run. Hard preconditions: baseline `witness.scan count=1 live=1 reserved0=0 reserved1=0` AND baseline `witness.scan-self count=0` (power-cycle Xbox if either condition fails). Sequence: ensure-agent → baseline both scans → reboot → FTP-upload both XBEs → ensure-agent → relaunch baseline scans → `runxbe witness-only` → FTP-LIST + agent/9001 + ICMP ping poll until dashboard returns (expect ~70 s on success per cycle 26 + cycle 28 timing) → ensure-agent → final both scans.

Expected outcomes E1/E1'/E1''/E2/E3/E4/E5 enumerated in `witness-only/README.md` cycle-30 discriminator table + `manifest.json` `expected_results.real-xbox/physical/cycle-30.notes`. On E1: γ INVALIDATED → cycle 31 option (b) for α-vs-β. On E2: γ LEADING → cycle 31+ option (d). On E3: full re-validation. On E4: redesign. On E5: power-cycle between attempts.

## Codex validation (cycle 29)

Cycle 29 ran 4 rounds per rule #15 (mandatory — non-trivial diff ~340 lines on renderer-adjacent diag-XBE + oracle-agent C source). Round 4 = LOOKS GOOD with no new findings; rounds 1-3 findings all RESOLVED. Validation marker recorded at `.claude/state/codex-validate-last-run`.

## Evidence on disk (cycle 29)

- No new `benchmark-runs/` directory this cycle (no real-Xbox run).
- Build artifacts:
  - `scripts/apple-silicon/xbe-tests/oracle-agent/{bin/default.xbe, oracle-agent.iso}` (rebuilt; oracle-agent size unchanged at 417 792 B).
  - `scripts/apple-silicon/xbe-tests/witness-only/{bin/default.xbe, witness-only.iso}` (rebuilt; witness-only +4 096 B = 151 552 B from cycle 25's 147 456 B; ISO unchanged at 720 896 B).
- Validation marker: `.claude/state/codex-validate-last-run` updated.

## Closure commit (cycle 29)

Pending on `apple-silicon-performance`.

## Cycle 28 design + outcome (locked at session close 2026-05-23)

| Step | Action | Outcome |
|---|---|---|
| 1 | Read required docs + state files + cycle-27/26 SUMMARY | Plan confirmed; canonical cycle-26-style sequence locked in |
| 2 | Reachability probe (08:44:29Z) | cycle-23 agent foreground (resident from cycle 26); FTP/21 closed; 9001 open |
| 3 | Baseline `witness.scan` | count=1 phys=0x03eb3000 reserved0=0 reserved1=0 mapped_pages_seen=419 — matches cycle 26 exactly; precondition MET |
| 4 | `reboot` agent → dashboard FTP-LIST | dashboard ready at t+27s |
| 5 | FTP-upload cycle-27 oracle-agent (417 792 B) | verified remote size; FTP LIST OK |
| 6 | Verify cycle-25 witness-only XBE still resident | 147 456 B present from cycle-26 deploy |
| 7 | `ensure-agent` launches cycle-27 build + rescan | SITE EXEC OK; ready in ~10s; banner unchanged (expected); rescan identical to baseline |
| 8 | `runxbe witness-only` + FTP-LIST poll | dashboard fully ready at t+70s (reproduces cycle 26's 70.17 s) |
| 9 | Post-run `ensure-agent` (cycle-27) + final `witness.scan` | **count=1 buf.0 phys=0x03eb3000 reserved0=0 reserved1=0** = outcome D-cycle-27 |
| 10 | Write SUMMARY.md + sync canonical docs | handoff.md + decision-log + orchestration-state quartet updated |
| 11 | Closure commit on `apple-silicon-performance` | `c77b509149` |

## Cycle 28 scope discipline

- ZERO xemu-fork host source touched.
- ZERO oracle-agent source touched.
- ZERO `lib/xbed_a4_witness.{c,h}` touched.
- ZERO witness-only source touched.
- ZERO image-blit source touched.
- ZERO XBE rebuilds.
- ZERO real-Xbox source/XBE deploys beyond the cycle-27 oracle-agent (already-built XBE binary).
- ZERO cycle-29 options (b)/(c)/(d) pursued.
- ZERO re-run of cycle-26's invalid-path or mirror controls (already characterized at 20.67 s and ~36 s).
- ZERO PushNotification — outcome is definitive partial-discriminator, not blocker / not milestone.
- Two pre-existing untracked `.hermes_cycle*.txt` prompt files at repo root preserved un-staged (consistent with cycle 26 + cycle 27 handling).

## Important interpretation notes (cycle 28)

- **The cycle-27 preserve branch is correctly wired up and deployed.** The cycle-28 outcome is "preserve branch had nothing to preserve," not "preserve branch defective." This is confirmed by: (a) Codex 3-round green at cycle-27 closure verifying the predicate logic, (b) the cycle-27 agent successfully restarts across the chainload and remains responsive, (c) the kernel-pool deterministic phys=0x03eb3000 reuse (cycle 28 alone observed ≥3 reuses; combined with cycle 26's 3, ≥6 consecutive reuses in same power session) confirms the preserve branch's preconditions are met every time. The strict predicate `(reserved0==0, reserved1==0)` OR `((reserved0>>24)==0xA4, 1<=reserved1<=4096)` covers both header shapes `xbed_a4_witness.c` actually writes; any landed stamp would have been preserved and observable.
- **The 70 s recovery shape is now a confirmed real-Xbox behavior** for `witness-only`, not a cycle-26 measurement artifact. Two-cycle reproducibility (~70.17 s + ~70 s).
- **The cycle-26 "stamp landed and got wiped" hypothesis is INVALIDATED.** The cycle-26 ambiguity collapses to "stamp never landed."
- **Three live causes for "stamp never landed":** (α) `xbed_a4_witness::a4_candidate_ok` kseg0 scan doesn't find agent's XCTR buffer from non-agent context, (β) scan finds it but write faults silently, (γ) witness-only's main() never reaches the fire calls. Cycle 28 cannot discriminate; cycle 29 option (c) or (d) is the path.
- **Cycle-22 leading hypothesis status: UNCHANGED.** Still WEAKENED. Cycle 28 evidence is equally consistent with (γ) (which would corroborate cycle 22) and with (α)/(β) (which would not). Discrimination requires cycle-29 instrumentation.

## Next bounded slice (cycle 29 — Hermes-scheduled)

**Recommended: option (c)** from cycle-27 closure's catalog. Modify `witness-only/main.c` to allocate its OWN persistent page via `MmAllocateContiguousMemoryEx` with a unique magic tag (separate from XCTR), stamp at known offsets within that self-allocated page, then `HalReturnToFirmware(HalRebootRoutine)`. No reliance on kseg0 scan finding the agent's buffer. Add a new read-only agent verb `witness.scan-foreign-magic` (or similar) that scans kseg0 for the unique magic and reports findings. Discriminates (α) from (γ):
- If (c) lands a stamp at the self-allocated page AND that page is findable by the new scanner, then witness-only's main() DID execute AND the kseg0-scan-from-non-agent-context mechanism IS sound — therefore (α) "agent buffer not findable from non-agent context" was the cycle-26/28 blocker (some specific property of the agent's allocation that the cycle-23 scan filter cannot match from non-agent context).
- If (c) lands nothing findable, (γ) "witness-only never reaches main()" becomes leading and cycle-22's pre-main-crash hypothesis re-strengthens. Cycle 30 should then move to option (d) for an independent main()-runs verification.

**Alternative: option (d)** if cycle 29 prefers a one-step discriminator for (γ) specifically. Add an on-screen visual breadcrumb to witness-only (synchronously emitted via debugPrint+pbkit-init OR a minimal NV097 single-poke write) and capture via `oracle-orchestrator.py capture` mid-run or composite-capture during witness-only execution.

Cycle 29 should also attempt to discriminate the cycle-26/28 ~70 s recovery shape (slow kseg0 scan vs delayed-fault watchdog vs slow BIOS POST). Option (c)'s own-page approach may collapse that ambiguity as a side-effect if a successful (c) run shows BIOS-POST-like timing (~17 s).

## Codex validation (cycle 28)

Cycle 28 is **run-only / doc-only** — ZERO source/script code edits, ZERO XBE rebuilds. Rule #15 trigger #2 (non-trivial uncommitted code in xemu-fork/) does NOT fire. The rule #15 doc-only / ≤30-line uncommitted source diff carve-out applies. **Codex SKIPPED**, consistent with cycle 26's handling.

## Evidence on disk (cycle 28)

`benchmark-runs/cycle28-real-xbox-witness-only-preserve-20260523T084331Z/` (gitignored per project convention) contains:
- `00-reachability.log`
- `01-agent-info.log`
- `02-baseline-witness-scan.log`
- `03-reboot-to-dashboard.log`
- `04-ftp-upload-oracle-agent.log`
- `05-ftp-list-witness-only.log`
- `06-relaunch-cycle27-agent.log`
- `07-chainload-witness-only-ftp-list.log`
- `08-postrun-witness-scan.log`
- `SUMMARY.md`

## Closure commit (cycle 28)

Landed on `apple-silicon-performance` as `c77b509149`.

## Cycle 27 design + outcome (locked at session close 2026-05-23)

| Step | Action | Outcome |
|---|---|---|
| 1 | Read required docs + state files + cycle-23/26 source | Plan confirmed; existing cycle-23 lockstep filter provides the contract for the cycle-27 preserve gate |
| 2 | Implement `s_page_has_plausible_witness_header` + two-branch `s_allocate_fresh` + debugPrint breadcrumb + paired doc updates | 5 files modified (controller.c substantive; controller.h + commands.c + witness-only/README.md + witness-only/manifest.json doc-only) |
| 3 | Rebuild oracle-agent XBE via nxdk make | bin/default.xbe 417 792 B (size unchanged); oracle-agent.iso 983 040 B |
| 4 | Codex validation round 1 (changes mode) | MINOR ISSUES: medium predicate-too-loose + low witness-only-docs-orphan-only-shape. Both adopted in full |
| 5 | Rebuild oracle-agent XBE after round-1 adoption | size still unchanged; manifest.json reparses cleanly |
| 6 | Codex validation round 2 | MINOR ISSUES: round-1 MEDIUM RESOLVED; round-1 LOW PARTIAL on one manifest field. Adopted |
| 7 | Codex validation round 3 | LOOKS GOOD: round-2 PARTIAL RESOLVED; round-1 MEDIUM still RESOLVED; no new findings |
| 8 | Write validation marker | `.claude/state/codex-validate-last-run` updated |
| 9 | Canonical docs sync | handoff.md cycle-27 entry on top; decision-log cycle-27 entry above cycle-26; orchestration-state quartet closure pass |
| 10 | Closure commit on `apple-silicon-performance` | Landed as `df999e41ea` |

## Cycle 27 scope discipline

- ZERO xemu-fork host source touched (no `hw/`, `ui/`, `target/`, `include/`).
- ZERO image-blit source touched.
- ZERO witness-only source touched (only paired doc edits in README.md + manifest.json).
- ZERO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 scan filter intentionally stays wider than the cycle-27 preserve gate; lockstep contract documented in three comment blocks).
- ZERO XBE rebuilds beyond `oracle-agent`.
- ZERO real-Xbox run.
- ZERO retail-title / §G.5 / RT-as-texture / second-wave-XBE work.
- ZERO flag default flips.
- ZERO cycle-27 options (b)/(c)/(d) pursued.
- Two pre-existing untracked `.hermes_cycle22_path_a3_prompt.txt` + `.hermes_cycle23_docsync_prompt.txt` at repo root preserved un-staged per Hermes pre-session instruction.

## Important interpretation notes

- The preserve gate is INTENTIONALLY a strict subset of the cycle-23 scan filter. A false negative just falls back to legacy full-zero behavior (safe; just the cycle-26 behavior we observed). A false positive would silently retain garbage as if it were a real witness header (would corrupt cycle-28 readback interpretation). Asymmetric failure modes justify the asymmetric strictness.
- Local validation in standalone xemu does NOT meaningfully exercise the preserve branch because xemu's `MmAllocateContiguousMemoryEx` + `MmPersistContiguousMemory` semantics don't deterministically reproduce the real-Xbox kernel-pool reuse pattern observed in cycle 26. Build success + 3-round Codex green is the appropriate high-confidence verification for this slice; the real-Xbox preserve-branch path is cycle-28 scope.
- The new positive success shape `count=1 live=1 reserved0=0xA4xxxxxx` and the legacy orphan shape are BOTH valid cycle-28 success outcomes. Operators reading any of the four updated locations (commands.c comment, witness-only README, manifest `artifacts.witness_readback.notes`, manifest `expected_results.real-xbox/physical/cycle-26.notes`) see both shapes; the ambiguity between "fire didn't land" and "fire landed but agent wiped it" is now resolvable from the next real-Xbox readback.
- The cycle-26 ~70 s recovery shape (slow kseg0 scan vs delayed-fault watchdog vs slow BIOS POST) is NOT addressed by the preserve branch. Cycle 28 should still discriminate this — the preserve branch only sharpens the post-chainload readback.

## Next bounded slice (cycle 28 — Hermes-scheduled)

Re-deploy the cycle-27 `oracle-agent/bin/default.xbe` (size unchanged at 417 792 B but preserve branch now live), reuse cycle-25 `witness-only/bin/default.xbe` as-is. Canonical sequence + the `curl FTP LIST` poll methodology that cycle 26 encoded. Power-cycle Xbox first if multiple A.4-tagged buffers pre-exist. Sequence: ensure-agent → baseline `witness.scan` precondition (count=1 live=1 reserved0=0) → FTP-upload new oracle-agent → relaunch agent → `runxbe witness-only` → poll FTP/21 + agent/9001 + ICMP ping → on dashboard return restart agent + query `witness.scan`. Expected positive shapes: A1 (`count=1 live=1 reserved0=0xA4000003`) — preserve branch retained the live-buffer stamp on the reused phys; A2 (`count>=2` with stamped orphan reserved0=0xA4000003) — legacy orphan shape if the kernel pool returned a different phys this time. Either A1 or A2 means cycle-22 leading hypothesis INVALIDATED. D-cycle-27 (`count=1 live=1 reserved0=0`) means the stamp never landed; cycle 29 promotes option (c) or (d). B (identical 928s+ hang) means hypothesis #5 promoted back; redesign required.

Cycle 28 should also re-attempt to discriminate the ~70 s recovery shape via an on-screen visual breadcrumb (`oracle-orchestrator.py capture` mid-run) or composite capture of the cycle-27 `debugPrint` breadcrumb during agent re-launch.

## Codex validation

Cycle 27 ran 3 rounds (rule #15 mandatory — non-trivial diff on renderer-adjacent oracle-agent C source). Round 3 = LOOKS GOOD with no new findings; round-1 + round-2 findings all RESOLVED. Validation marker recorded.

## Evidence on disk

- No new `benchmark-runs/` directory this cycle (no real-Xbox run; no XBE harness sweep).
- Build artifacts: `scripts/apple-silicon/xbe-tests/oracle-agent/{bin/default.xbe, oracle-agent.iso}` (committed; size unchanged from cycle 26 but content differs).

## Closure commit

Landed as `df999e41ea` on `apple-silicon-performance`.
