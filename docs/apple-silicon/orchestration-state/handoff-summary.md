# Handoff Summary

- Previous cycle: cycle 14 closed cleanly and showed that the GL screenshot path is upstream-blocked for this diagnostic XBE, so it could not decide the renderer-agnostic race question.
- Latest cycle: cycle 15 closed 2026-05-22 12:46 CDT under `hermes_xemu_live_20260522_121700`. A new opt-in host-visible guest-log channel (`XEMU_GUEST_LOG=1`, fixed IO port 0xE9, `xemu-guest-log:` prefix in stderr) landed alongside `image-blit` v0.4 which emits per-cell PASS|FAIL + first-mismatch + final tally through the new channel. First captured run shows both Metal and GL emit identical first-invocation tally `pass=3/8 mask=0x31`, **confirming the cycle-13 PFIFO ↔ vCPU race hypothesis is renderer-agnostic** (the §H.6 residual lives in shared PFIFO/PGRAPH machinery upstream of either renderer).
- Why this matters: cycle 11 follow-up item #1 ("Re-run image-blit on GL") is now CLOSED with decisive evidence. The §H.6 close path has narrowed to a single small `hw/xbox/nv2a/` change: cycle-13 follow-up item #2 (`XEMU_DIAG_PGRAPH_STATUS_DRAIN`), expected to flip all 8 cells green on both renderers.
- Closure gates met: worker receipt; bounded implementation; fresh run evidence (`benchmark-runs/xbe-cycle15-host-log-20260522-123038/`); mandatory Codex validation (MAJOR ISSUES, all three findings adopted); canonical doc sync (handoff.md head + cycle-15 section, decision-log cycle-15 entry, automation.md new section, flags-bench.md index entry, manifest.json v0.4, orchestration-state files closed in this commit).
- Next bounded slice: implement `XEMU_DIAG_PGRAPH_STATUS_DRAIN` and verify via cycle-15 host-log channel that mask flips to 0xFF on every reboot under both renderers. Codex MANDATORY.
