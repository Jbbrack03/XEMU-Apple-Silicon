# Handoff Summary

- Cycle 17 closed the local renderer-agnostic milestone; cycle 18 packaged the state in a separate doc-only checkpoint.
- Cycle 19 attempted a real-Xbox parity check via the cycle-15 v0.4 image-blit XBE; reproducibly produced zero `D:\image-blit-capture.bin` / `D:\image-blit-done.txt` across two runs; cycle-17's xemu-side `pass=8/8 mask=0xff` could not be cross-witnessed. `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on decision DEFERRED.
- Cycle 20 took Path A (image-blit instrumentation). Added 13 staged marker calls writing to `D:\image-blit-marker-NN-STAGE.txt`. Local xemu validation showed all 13 markers fire via host-log channel; every `fopen("D:\\…","wb")` returns NULL on the ISO mount (CD-ROM, read-only). Two real-Xbox runs (pre-Codex labels, post-Codex shortened labels) BOTH produced zero marker files; chainload→FTP-back stable at 22.4 s. Conservative reading: D:\ write-back blocked under `runxbe` SITE-EXEC chainload (cycle-19 hypothesis #1 elevated to leading).
- **Cycle 21 took Path A.2 (the cycle-20-identified cheapest single follow-up): re-route the 13 markers from `D:\` to `E:\Apps\image-blit\…` — the xbe-harness's existing `--ftp-collect /E/Apps/<id>` target.** Added idempotent E:\ mount + `CreateDirectoryA` chain (verbatim pattern from `oracle-agent/controller.c` + three other XBEs). Local xemu-Metal validation across 4 boots: 52 marker host-log lines, all `e_mount=1`, **zero `fopen-failed`** (vs cycle-20's 52/52 fopen-failed); v0.4 tally drift byte-identical to cycle 20 (no regression). **Single real-Xbox run produced a clear NEGATIVE answer: chainload→FTP-back 22.4 s (4th independent reproduction across cycle-19+cycle-20+cycle-21), `verdict.json status: ok`, FTP-collect retrieved exactly 1 file (`default.xbe` upload echo, 159 744 B), ZERO `image-blit-marker-*` files.**
- **Conservative interpretation (cycle 21).** Cycle-19 hypothesis #1 ("D:\ remap mismatch under `runxbe` chainload is the witness-path blocker") is DEMOTED from leading to **insufficient as a sole explanation** — re-routing to a provably-writeable, provably-retrievable E:\ path does not unblock the witness either. The failure mode is upstream of any in-XBE `fopen` call. Hypotheses #2/#3/#4 (NV2A class-object instantiation mismatch / `pb_agp_access` divergence / generic early-init failure) weight goes UP but cycle 21 does not directly discriminate them. New cycle-21 hypothesis recorded: the harness FTP UPLOAD step makes `E:\Apps\image-blit\` writeable from FTP-server-time perspective; the chainloaded XBE may run in an environment where FATX-driver / NT-mount state differs from FTP-server-time state (speculative; needs evidence).
- `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated. The flag continues to ship opt-in, default OFF.
- Codex validation passed cycle 21 (mode `changes`, verdict MINOR ISSUES, two findings about doc-sync completeness at Codex-run-time — both adopted in full in the same closure pass). Validation marker written at `.claude/state/codex-validate-last-run`.
- **Cycle 21 closure commit:** `df8eb65efc` (2026-05-22). Slice intended-scope-only: `scripts/apple-silicon/xbe-tests/image-blit/main.c` marker re-route (~120 net added lines) + rebuilt `bin/default.xbe` + `image-blit.iso` by-products + canonical doc syncs (`handoff.md`, `decision-log.md`, all four orchestration-state files).
- **Next bounded slice (NOT promoted this cycle):**
  - **A.3 (now top-priority per rule #1).** Cross-check whether the existing `xbox-real-references/{pipeline-smoke,mirror,color-channel,depth-floor}` captures came from `runxbe` SITE-EXEC chainload or from a different launch path. Cheapest single follow-up: requires only inspecting existing artifacts + tooling history; no new code. If from `runxbe`, the cycle-21 result is image-blit-specific; if from a different launch path, the oracle pipeline needs a non-`runxbe` chainload mode.
  - **A.4 (new, proposed cycle 21).** Add a non-fopen witness to image-blit: write a few bytes into the oracle-agent's persistent kernel-pool controller buffer (`oracle_ctrl_buffer`) BEFORE attempting any marker fopen. Next agent boot reads the buffer via `controller.buffer-info` / `controller.get`. Bypasses every partition-mount + FATX-driver-state concern. Heavier than A.3 but adds independent high-signal evidence.
  - **B.** Smaller PFIFO-race-only Tier-1 diag XBE that captures via PCRTC. Still on the table; heavier than A.3 / A.4.
- All evidence and canonical-doc updates are on disk; the next session can resume cold from files per the orchestration-workflow.md §7 model.
