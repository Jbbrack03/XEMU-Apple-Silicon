# Handoff Summary

- Cycle: 2026-05-21 evening, Hermes-supervised cycle 3 (task #16 render-target attribution slice).
- Owner: Claude Code.
- Outcome: Exit option A — bounded diagnostic slice that decisively closes the cycle-2 open question. Tree state: 1 source file modified (`hw/xbox/nv2a/pgraph/mtl/renderer.c`, +43 LOC env-gated diag, zero impact when env unset), 5 doc files updated (`handoff.md` cycle-3 banner appended, `decision-log.md` cycle-3 entry, `automation.md` diag-toggle, `../.claude/rules/flags-renderer.md`, plus orchestration-state), new evidence directory `docs/apple-silicon/task-16-evidence-2026-05-21/cycle3-replay/` (README + 4 GLSL dumps + 6 log files + 2 representative screenshots). Codex validation PASS with MINOR ISSUES (single publish-histogram arithmetic finding adopted in-slice). Ready to commit; do NOT push to origin.
- Key findings (durable, see `handoff.md` task #16 cycle-3 banner + `decision-log.md` cycle-3 entry):
  1. The XBE's 32 captured stride==44 `xbed_draw_arrays(TRIANGLES, cell*24, 24)` dispatches all render to back-buffer-class color targets `0x03aa8000`, `0x03bd4000`, `0x03d00000` cycling round-robin per frame; depth target uniformly `0x0397c000`.
  2. The XBE NEVER renders to `0x032a4000` (0/32 stride==44 dispatches). The replayed `0x032a4000` GLSL dump is a uniform-only draw (every slot reads from `inlineValue[N]`, `uniform_attrs == 0xFFFF`) — not the XBE's pipeline.
  3. Under `XEMU_METAL_FRONT_FB_FALLBACK=1` (xbe-harness canonical Metal recipe) the publish path uses dominant-draw fallback, which 88% of the time picks the dashboard surface `0x3628000` (~10000 draws/interval to a single buffer) over the XBE's 3 back buffers (~700 draws/interval each). Only 3/427 publishes go to an XBE back buffer — sufficient for the harness best-frame selector to land an XBE-content screenshot.
  4. The captured "best frame" 0124 IS the XBE's actual output: 4×2 cell grid with per-mip RED tint ramp (LOD-clamp working) and intra-mip Q0 collapse (the canonical Task #16 symptom). The cycle-2 banner's "corner-tinted gradient covering the full surface" description was capturing a different surface — most likely the BIOS animation routed through the M5.13 VGA-direct deferred path.
  5. CPU-side unswizzle is correct for every mip (4 distinct quadrant colors per mip). Combined with cycle 2's per-vertex slot-9 stream + uniform_attrs proofs, this localizes the bug to the **Metal texture sampler / fragment-shader UV-to-texel path** — DOWNSTREAM of everything cycle 2 ruled out and everything cycle 3 just ruled out.
- Prior narrative superseded: cycle 2's "(a) XBE renders to back buffer, screenshot publishes a stale front-buffer pipeline / (b) XBE renders to front buffer directly" hypothesis pair. Cycle 3 confirms (a) and rules out (b). The "front-buffer pipeline with v3 uniform + v9 streaming" cycle 2 sub-narrative is also superseded: the actual `0x032a4000` pipeline today is fully uniform-only.
- New tooling shipped (env-gated): `XEMU_METAL_DIAG_ATTRIB_DUMP` extended with a fourth `metal_dispatch_draw_target` log stream (one line per stride==44 dispatch, 32-line cap matched to existing `metal_set_attr_masks` cap so they interleave 1:1).
- Next slice's highest-value experiments (suggested; not binding): (1) per-cell sampler-state attribution diag in `pgraph_mtl_texture_bind_from_pg` gated on the same stride==44 heuristic, logging resolved `min_lod_clamp`/`max_lod_clamp`/`mip_filter`/etc. per draw; (2) use `XEMU_METAL_DUMP_DRAW_RT` to dump post-draw color RT per-cell and isolate sampler-vs-publish bug; (3) consider an XBE-specific `metal_canonical_overrides` setting `XEMU_METAL_FRONT_FB_FALLBACK=0` so the swizzle-mipmap publish goes through CRTC resolution.
- Codex review state: PASS (single MINOR finding adopted; histogram arithmetic in 3 docs corrected to match staged log exactly). Marker `~/.claude/state/codex-validate-last-run` written. Safe to commit.
