# Handoff Summary

- Cycle: 2026-05-22, Hermes-supervised cycle 6 (§4.13 `texture-shader-stages` v0.1 slice).
- Owner: Claude Code.
- Outcome: **Exit Option B — bounded partial closed cleanly.** §4.13 `texture-shader-stages` v0.1 shipped as SPEC ORACLE; last unstarted first-wave XBE is now in the rotation. The XBE is design-verified (Codex MINOR ISSUES adopted; no authoring bug found) but FAILs on xemu/metal with a documented signature (filed as task #18 for next-session renderer investigation). First-wave Metal XBE PASS count: **16 of 17 → 16 of 18** (one new expected_fail SPEC oracle, mirroring the swizzle-mipmap v0.2 / logic-ops pattern). Zero regressions in the cycle's two sanity runs (combiner-basic PASSed on the same harness setup in the same session). Tree state at closure: 5 new files under `scripts/apple-silicon/xbe-tests/texture-shader-stages/` (main.c, expected.py, manifest.json, Makefile, plus build artifacts) + 4 doc updates (handoff cycle-6 banner, decision-log cycle-6 entry, 4 orchestration-state files refreshed). Ready to commit; do NOT push to origin.
- Key changes shipped (durable):
  1. **§4.13 `texture-shader-stages` v0.1 XBE.** 4x2 grid (8 cells) covering 2 of 19 NV2A SHADER_STAGE_PROGRAM modes at stage 0: `PASS_THROUGH` (0x04) on row 0 with per-cell `TEXCOORD0 = (R,G,B,1)` → t0 = pT0 → R0 → fragColor; `PROGRAM_NONE` (0x00) on row 1 with identical TEXCOORD0 → t0 = (0,0,0,1) → BLACK. Shared explicit combiner override mirrors `combiner-basic` topology (A_SOURCE=T0, AB_DST=R0, FINAL D=R0, G=DIFFUSE.a). Stage 0 bound to a 4x4 magenta dummy texture so the renderer does not force stage_program=NONE due to a disabled stage (per `psh.c:142-148`); the texture is never sampled by either mode.
  2. **Math-derived `expected.py` oracle.** Saturated 0/255 cube-corner channels per cell; cell interiors byte-exact in the float→8-bit framebuffer quantize step.
  3. **`manifest.json` with `expected_fail_renderers: ["xemu/metal"]`.** Detailed `expected_fail_notes` documents the empirical Metal FAIL signature (boots OK, clears to BLACK OK, 8 per-cell draws produce no visible output; 3 distinct render-and-reboot cycles observed), the 3 candidate root causes for task #18 (one ruled out by Codex during this review's verification of the pipeline cache key composition), the GL exclusion rationale (harness screencap path limitation), and the next-session-actionable v0.2 bisect experiment (add a DIFFUSE-source variant cell to isolate XBE-side vs renderer-side root cause). Pins `metal_canonical_overrides: {"XEMU_METAL_SCREENSHOT_SOURCE": "nv2a"}` so captures land on the 640x480 NV2A surface.
- Prior narrative superseded: 2026-05-22 (Hermes cycle 5)'s "Net next-highest-value actions" item 1 ("Author §4.13 `texture-shader-stages` — the last unstarted first-wave XBE") is now the implemented decision. The v0.1 covers 2 of 19 modes; the remaining 17 modes are deferred to v0.2+ behind multi-stage chaining infrastructure.
- New tooling shipped: NONE (no new env flags or counters — the slice ships only the new diag XBE + manifest + doc updates).
- Validation evidence (durable, all under `benchmark-runs/`):
  - `20260522T064552Z-task18-texture-shader-stages-metal/` — initial Metal run with default `drawable` screenshot source. Shows the FAIL pure-BLACK output.
  - `20260522T065933Z-task18-ts-shader-dump/` — `XEMU_METAL_DUMP_TARGET_SHADER=all` run. 1024 .glsl files dumped; 2 unique pipelines for the 0x032a4000 front buffer, both PROJECT2D-mode (from dashboard + `xbed_load_textured_shaders` Cg setup); zero PSH variants with `vec4 t0 = pT0;`.
  - `20260522T070256Z-task18-texture-shader-stages-metal-v0.1-baseline/` — canonical baseline with `XEMU_METAL_SCREENSHOT_SOURCE=nv2a` pinned via manifest's `metal_canonical_overrides`. Harness correctly reports `expected_fail` (signal_match_pct=19.3 confirms no per-cell color content reaches the front buffer).
  - Same-session sanity: `/tmp/combiner-basic-sanity/` PASS — confirms harness itself is sound.
- Next slice's highest-value experiments (suggested; not binding):
  1. **Investigate task #18 (§4.13 Metal silent-fail root cause).** Three candidate root causes (one ruled out by Codex; two remain plausible): (a) `NV_PGRAPH_SHADERPROG` may not trigger pipeline-rebuild dirty-state on Metal despite being in the cache key; (b) XBE-side combiner setup may cause silent failure even though the encoding matches `combiner-basic`; (c) state-machine interaction between `xbed_load_textured_shaders` Cg setup and per-cell override. Next concrete code step: author the v0.2 DIFFUSE-source bisect cell described in the manifest's `expected_fail_notes`.
  2. **Investigate task #17 (GL LOD-clamp regression)** — separate slice; pre-existing.
  3. **v0.2 expansion of `texture-shader-stages`** after task #18 closure: add the remaining 17 modes (PROJECT2D, PROJECT3D, CUBEMAP, CLIPPLANE, BUMPENVMAP*, BRDF, DOT_*, DPNDNT_*, DOTPRODUCT, DOT_RFLCT_SPEC_CONST) with the multi-stage chaining infrastructure that's been deferred.
  4. **Investigate the pipeline-smoke `surface_scale=2` leak** — harness / xemu.toml interaction; pre-existing, not regressed by this slice.
- Codex review state: COMPLETED. Verdict: MINOR ISSUES.
  - MEDIUM (adopted): the v0.1 manifest's original candidate root cause (1) "Metal pipeline cache key omits SHADER_STAGE_PROGRAM" — Codex verified the key DOES include it via `hw/xbox/nv2a/pgraph/mtl/shaderstate.h:59-67` → `hw/xbox/nv2a/pgraph/glsl/shaders.h:27-31` → `hw/xbox/nv2a/pgraph/glsl/psh.h:37-40`. Manifest rewritten: revised hypothesis (1) now points at pipeline-rebuild dirty-state propagation around `NV_PGRAPH_SHADERPROG` writes, not key omission.
  - LOW (adopted): `main.c` header text + `expected.py` docstring previously claimed "no `compare_overrides` needed / byte-exact" while the manifest sets `max_changed_pct=3.0 / min_signal_match_pct=97.0`. Both header texts updated to clarify the budget exists only to absorb inter-cell rasterizer edges (4 vertical seams + 1 horizontal mid-line) and harness frame-selection slack; the per-channel threshold remains the harness default (16); cell interiors are byte-exact.
  - OPEN question (deflected via explicit documentation): Codex asked whether GL should be explicitly encoded in `expected_fail_renderers`. Manifest now documents the GL exclusion explicitly in `expected_fail_notes`: the harness GL screencap path reports `no-screenshot-captured` rather than a meaningful diff; treat any GL run as smoke until a renderer-native GL screenshot path lands.
  - OUT OF SCOPE Codex noted: no obvious authoring bug in `main.c` explains the all-BLACK Metal result. The combiner / source encodings and PASSTHRU/NONE derivation are internally consistent against `psh.c`. This strengthens the case that the FAIL is renderer-side and validates filing it as task #18 (renderer investigation) rather than reworking the XBE.
  - Marker `/Users/jbbrack03/XEMU_MacOS/.claude/state/codex-validate-last-run` will be written on the post-commit clean fingerprint.
