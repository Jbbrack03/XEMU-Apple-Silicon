# Handoff Summary

- Cycle 17 closed the local renderer-agnostic milestone; cycle 18 packaged the state in a separate doc-only checkpoint.
- Cycle 19 attempted a real-Xbox parity check via the cycle-15 v0.4 image-blit XBE; reproducibly produced zero `D:\image-blit-capture.bin` / `D:\image-blit-done.txt` across two runs; cycle-17's xemu-side `pass=8/8 mask=0xff` could not be cross-witnessed. `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on decision DEFERRED.
- Cycle 20 took Path A (image-blit instrumentation). Added a best-effort helper `image_blit_marker(idx, stage)` and 13 staged calls at every meaningful checkpoint in `main()` from "first line, before xbed_init" through "last call before xbed_render_loop_then_capture". Local xemu-Metal validation (with XEMU_GUEST_LOG=1) confirms ALL 13 markers fire via the host-log channel AND every `fopen("D:\\…","wb")` returns NULL on the ISO mount path (CD-ROM, read-only). Two real-Xbox runs (one pre-Codex labels, one post-Codex shortened labels per the FATX 42-char limit) BOTH produced zero marker files. chainload→FTP-back stable at 22.4 s across all three cycle-19+cycle-20 real-Xbox runs (third reproducibility confirmation).
- Conservative interpretation: cycle-19 hypothesis #1 (D:\ remap mismatch under `runxbe` SITE-EXEC chainload) is promoted to leading hypothesis. Hypotheses #2/#3/#4 are NOT discriminated by cycle-20 evidence — all four share the same surface observation once #1 is true. Explicitly NOT claimed: that the XBE crashes at any specific stage, that the leading hypothesis is universal across all `runxbe`-launched XBEs (existing `xbox-real-references/` captures may have used a different launch path — Path A.3 should cross-check before treating #1 as a universal fact).
- `XEMU_DIAG_PGRAPH_STATUS_DRAIN` default-on / long-term-fix decision REMAINS DEFERRED. Cycle-17's xemu-side `8/8 mask=0xff` finding is NOT invalidated. The flag continues to ship opt-in, default OFF.
- Codex validation passed (mode `changes`, verdict MINOR ISSUES, one medium-severity FATX-overflow finding adopted in full). Validation marker written at `.claude/state/codex-validate-last-run`.
- Diff at session close is bounded: `main.c` (+101 net lines), rebuilt `bin/default.xbe` + `image-blit.iso`, and doc-sync edits to `handoff.md`, `decision-log.md`, and the four orchestration-state files. Whether to commit the slice in-session or close to a documented uncommitted state is the caller's choice.
- Next bounded slice (cycle 20+): Path A.2 (re-route markers to a non-D:\ partition), Path A.3 (cross-check existing real-Xbox-reference provenance), or Path B (smaller PFIFO-race-only Tier-1 diag XBE). Scope choice belongs to the next Hermes pass.
- All evidence and canonical-doc updates are on disk; the next session can resume cold from files per the orchestration-workflow.md §7 model.
