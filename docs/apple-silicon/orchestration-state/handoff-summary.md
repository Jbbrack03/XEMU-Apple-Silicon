# Handoff Summary

- Closed: cycle-13 cleanup continuation `hermes_xemu_live_20260522_110134` landed cleanly 2026-05-22 as a doc-only §H.6 `image-blit` residual code-path audit + state-sync closure.
- Shipped: §H.6 `image-blit` residual reframed from cycle-12's "guest CPU cache-coherency" framing to a **PFIFO ↔ vCPU dispatch race against the never-published `NV_PGRAPH_STATUS`** (default-on `XEMU_PGRAPH_FAST_READ` returns the zero-initialized bit via `__ATOMIC_RELAXED`; pbkit's `pb_wait_until_gr_not_busy` is `while(0) {}`; PFIFO kick is pure async signal). All load-bearing file/line citations spot-verified against the live tree. Renderer source diff = 0; Codex NOT required (rule #15 doc-only exemption). Slice commit hash: `0bd85f70fe` (recorded in this follow-up state-sync commit).
- Next bounded slice: cycle 14 — GL-leg replay of `image-blit.iso` through `scripts/apple-silicon/xbe-harness` under `XEMU_RENDERER=GL`. The dispatch-race hypothesis predicts the same PASS/FAIL split on GL renderer-agnostically. See `decision-log.md` 2026-05-22 cycle-13 entry "Cycle-14 next-slice plan" for the staged follow-on slices (`XEMU_DIAG_PGRAPH_STATUS_DRAIN=1` diagnostic flag, real-Xbox oracle parity check).
- Prior closed-cycle technical narrative preserved in `handoff.md` (2026-05-22 cycle-13 banner block) and `decision-log.md` (2026-05-22 cycle-13 entry). Cycle-12 v0.3 BOUNDED PARTIAL conclusions remain valid in direction; cycle-13 sharpens the mechanism.
