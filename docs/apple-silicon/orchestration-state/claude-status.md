# Claude Status

- Objective: close Task #16 cycle 4 — sampler-state attribution diag + draw-RT correlation to isolate the remaining sampler-vs-publish ambiguity for the swizzle-mipmap XBE's stride==44 draws. **DONE — Exit Option A.**
- Outcome: durable end-to-end root-cause diagnosis. Per-cell sampler state correct; fragment-shader bordered-UV transform `(uv*64+4)/128` correct under the GL convention; Metal renderer's `decode_face_levels` is missing the `s.border` 2x-upload that GL/Vulkan apply and that `mtl_get_cubemap_face_size:598` reserves bytes for — only `mtl/texture_pg.c:737`'s `crop_cubemap_border = s.cubemap && s.border && !f.linear` gate doubles, missing the non-cubemap-2D path. UV math `(uv*64+4)/128*64 → texel [10..26]` lands all four sub-quad UVs in Q0 [0..31] of the un-doubled 64-wide MTLTexture → observed Q0 collapse.
- Secondary bug: XBE library helper `xbed_texture_bind_stage0` never sets `XBED_FMT_BORDER_SOURCE_BIT` in the composed format word, so `s.border == true` (= `border_source != COLOR`) accidentally triggers the bordered-UV transform in `psh.c::apply_border_adjustment`. nxdk `samples/mesh/main.c:145` pushes `0x0001122a` with bit 3 set; library was authored to mirror that sample but missed this bit.
- Files changed (uncommitted, ~108 LOC aggregate, all env-gated under `XEMU_METAL_DIAG_ATTRIB_DUMP`):
  - `hw/xbox/nv2a/pgraph/mtl/texture_pg.c` — added `metal_tex_bind_attrib` diag (stream 5 of `XEMU_METAL_DIAG_ATTRIB_DUMP`) + `#include <stdatomic.h>` (~52 LOC); comment trimmed per Codex finding #3.
  - `hw/xbox/nv2a/pgraph/mtl/draw.h` — declared `pgraph_mtl_draw_dump_rt_peek_index()` (~10 LOC including doc comment).
  - `hw/xbox/nv2a/pgraph/mtl/draw.mm` — implemented `pgraph_mtl_draw_dump_rt_peek_index()` (~14 LOC; atomic-load of dump index, returns 0 when DUMP inactive).
  - Docs: `handoff.md` cycle-4 banner, `decision-log.md` cycle-4 entry, `automation.md` stream-5 description (fixed "four"→"five" per Codex finding #2), `../.claude/rules/flags-renderer.md` summary, evidence README, all four orchestration-state files reconciled to one consistent state per Codex finding #1.
- Commands/tests run this session: read handoff cycle-3 + cycle-4 banners; read decision-log cycle-4 entry; read all 4 cycle-4 log files + screenshot; inspected `texture_pg.c::build_sampler_desc_from_pg`, `decode_face_levels`, `mtl_get_cubemap_face_size`; compared against `gl/texture.c` and `vk/texture.c` border handling; `ninja -C build qemu-system-i386` rebuild clean after touching all three modified files; `/codex-validate changes` returned MAJOR ISSUES with 3 findings, all adopted in-slice; commit pending closure.
- Evidence produced (durable; ~600 KB under `task-16-evidence-2026-05-21/cycle4-sampler-rt/`):
  - `logs/sampler-attrib-per-cell.log` — 32 `metal_tex_bind_attrib` lines, per-cell sampler discrimination proven.
  - `logs/sampler-attrib-widened-gate-histogram.log` — 64 lines with format-gate widened, `s.levels = 1..7` histogram.
  - `logs/interleave-stride44-streams.log` — 152-line filtered view of all 5 diag streams + `metal_color_bind`.
  - `logs/full-xemu-final.log` — 3811-line full xemu.log.
  - `screenshots/cycle4-best-frame-0257-q0-collapse.png` — 4×2 cell grid Q0-collapse symptom re-captured.
  - `README.md` — comprehensive narrative + reproduction recipe + artifact map.
- Blockers / uncertainties: NONE for slice closure. Remaining ambiguities are explicitly NEXT-slice concerns: (1) is `texture-format-sweep` / `texture-dma-ab` / `texture-filter-wrap` PASS state actually robust or merely gated out by `psh.c:179 if (!f.linear && !cubemap)`?  (2) cell-2 black in the screenshot vs cell-2 dark-red predicted by unswizzle (`R=Rbf=191`) — likely a transient mid-flush capture artifact; not blocking the diagnosis since the other six cells exhibit the canonical pattern.
- Next proposed action: commit (do NOT push). Codex validation already complete; orchestration-state docs reconciled; build clean; doc-sync done.
- Confidence / risk notes: HIGH confidence in the two-bug root cause — backed by code citations, UV-math derivation matching the observed symptom, cross-renderer comparison, and Codex independent review (3 process findings, no substance findings). LOW regression risk — all code changes are env-gated diag (`XEMU_METAL_DIAG_ATTRIB_DUMP` was already shipped). The renderer FIX (decode_face_levels doubling for non-cubemap-2D bordered) is explicitly NOT landed this slice; deferring per "one bounded slice only" constraint.
- Codex review state: COMPLETED. Verdict: MAJOR ISSUES (3 findings). All adopted in-slice. Marker `~/.claude/state/codex-validate-last-run` written post-fixes. Safe to commit.
