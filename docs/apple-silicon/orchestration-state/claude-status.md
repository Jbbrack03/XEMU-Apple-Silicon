# Claude Status

- Objective: investigate task #18 by authoring + running the v0.2 DIFFUSE-source bisect variant of §4.13 `texture-shader-stages`. If evidence localizes the bug, attempt a minimal renderer-side fix; otherwise close as bounded partial.
- Status: SLICE LANDED + BUILT + RUN + HARNESS-VALIDATED AS EXPECTED_FAIL (Metal). v0.2 expands the 4x2 grid to 4x3 by adding row 2 (cells 8-11) — `SHADER_STAGE_PROGRAM=PROGRAM_NONE` for stage 0 (same as row 1) + combiner ICW stage-0 A_SOURCE rewired from T0 (0x8) to V0 (PS_REGISTER_V0 = 0x4, DIFFUSE), with per-cell DIFFUSE attribute = (R,G,B,1). Row 2 bypasses the t0/pT0 chain entirely — its expected output mirrors row 0 (RED/GREEN/BLUE/WHITE) iff the combiner program + vertex-attribute path is sound and only the t0/PASS_THROUGH chain is broken.
- Files touched (4 modified):
  - `scripts/apple-silicon/xbe-tests/texture-shader-stages/main.c` — 4x3 grid (12 cells), per-row combiner switch (`program_combiners_with_a_source`), per-cell DIFFUSE attribute via `ShaderStageCell.diffuse[4]`, v0.2 header narrative.
  - `scripts/apple-silicon/xbe-tests/texture-shader-stages/expected.py` — 12-cell layout, row 2 R/G/B/W from DIFFUSE.
  - `scripts/apple-silicon/xbe-tests/texture-shader-stages/manifest.json` — v0.2 title; rewritten `expected_fail_notes` with the bisect verdict; compare overrides relaxed from 3.0/97.0 to 5.0/95.0 for the extra row's rasterizer seams.
  - `scripts/apple-silicon/xbe-tests/texture-shader-stages/{bin/default.xbe, texture-shader-stages.iso, main.exe, main.obj, main.c.d}` — rebuilt artifacts (clean nxdk build).
- Commands/tests run:
  - `make` in the texture-shader-stages dir with nxdk activated → clean build (all libs rebuilt; CXBE + XISO succeeded; one pre-existing Cg warning about TEX0 register mapping is unchanged from v0.1).
  - `python3 expected.py /tmp/texture-shader-stages-v02-expected.png` → 640×480 PNG with the 3-row layout (RED/GREEN/BLUE/WHITE on rows 0 + 2; BLACK on row 1).
  - `python3 xbe_orchestrator.py list` → confirmed XBE is discovered (still 20 XBEs; title updated to mention v0.2).
  - `python3 xbe_orchestrator.py run --xbe texture-shader-stages --renderer metal` → `expected_fail` per manifest (manifest declares the metal expected_fail).
- Evidence produced:
  - `benchmark-runs/20260522T075639Z-task18-texture-shader-stages-metal-v0.2-baseline/` — full 256-PNG screenshot sequence + `report.md` + `summary.json` + `texture-shader-stages/metal/{screenshots/, xemu.log, xemu.toml, reference.png, compare/, ...}` + the harness-rebuilt math-derived expected PNG at `texture-shader-stages/metal/texture-shader-stages-v02-expected.png` + `key-evidence/` dir with 4 representative frames (0044 dashboard, 0116/0183/0248 XBE-active BLACK) + `key-evidence/per-row-stats.md` narrative.
- Empirical finding (v0.2 bisect verdict — Metal; refined per Codex 2026-05-22 findings 1+2):
  - **Row 2 ALSO produces pure (0,0,0,0).** 8 XBE-active host-capture frames across 3 distinct render-and-reboot cycles (indices 0116-0118, 0183-0184, 0248-0250) are unique-color = 1 with RGB max = 0 per channel. Per-row means at every BLACK frame: row 0 R=G=B=0.0, row 1 R=G=B=0.0, row 2 R=G=B=0.0. Zero non-zero pixels anywhere.
  - **PASS_THROUGH-specific reading of v0.1 hypothesis 1 RULED OUT.** Row 2 keeps SHADER_STAGE_PROGRAM at PROGRAM_NONE for all 4 cells (no inter-cell change), so a SHADER_STAGE_PROGRAM-mode-specific dirty-state propagation bug cannot explain row 2's failure. **Broader reading remains live** (per Codex finding 1): "any SHADER_STAGE_PROGRAM override under xbed_load_textured_shaders() is not being honored" needs the v0.3 control-row bisect or per-draw pipeline-key dump to confirm or rule out.
  - **Surviving candidates:**
    - (a, renamed v0.1 candidate 3) State-machine interaction between `xbed_load_textured_shaders` Cg-emitted setup and the XBE's per-cell overrides on Metal. The dashboard renders correctly and `combiner-basic` (uses `xbed_load_default_shaders`, no texturing) PASSes in the same harness session, but every per-cell draw in `texture-shader-stages` fails on Metal. Suspect interaction points: (a.i) Cg's pre-set `SHADER_STAGE_PROGRAM=2D_PROJECTIVE` for stage 0 in `xbed_tex_ps.inl`; (a.ii) textured VS's 3 input attributes vs default VS's 2; (a.iii) `bind_dummy_stage0`'s tex bind interaction with per-cell SHADER_STAGE_PROGRAM override.
    - (b, NEW per Codex 2026-05-22 finding 2) **Combiner-rewrite ignored under textured-shader state.** The only intentional row1→row2 delta is the second `program_combiners_with_a_source()` call switching `ICW_A_SOURCE` from T0 to V0. If that combiner update is silently dropped under textured-shader state, row 2's BLACK output is equally explained by row 1's residual `A_SOURCE=T0` config still in effect (combined with `t0=(0,0,0,1)` from NONE → `R0=0` → BLACK). Distinct from (a): even if SHADER_STAGE_PROGRAM overrides ARE honored, the combiner override may not be. Next slice should instrument the row1→row2 `A_SOURCE` switch directly with a sentinel combiner config that would produce a deterministic non-black output IF the combiner update is honored.
- Separate bug identified (NOT task #18 itself but real): `hw/xbox/nv2a/pgraph/glsl/psh.c:142-148` + `hw/xbox/nv2a/pgraph/pgraph.h:330` (`pgraph_is_texture_stage_active`) degrades PASS_THROUGH (mode 0x04) to NONE in `state->shader_stage_program` because the helper returns false for mode 4 and the fork-local gate (commit `046160d04d`, 2026-05-04) clears the per-stage mode bits when `!enabled`. This explains the v0.1 shader-dump finding (no PASSTHRU signature emitted for row 0) but does NOT explain row 2's failure. The minimal proposed fix would exempt NONE and PASS_THROUGH from the zeroing gate; shared GL+Metal code. Queued as a separate future slice with its own Metal-boot regression test rather than landed in cycle 7 because (a) it wouldn't make task #18 PASS on Metal, (b) commit `046160d04d` was specifically a Metal-boot stability fix and naïvely relaxing the gate without bisecting boot stability could regress.
- Blockers / uncertainties: task #18 root cause now narrowed to two surviving candidates (textured-shader state-machine interaction; combiner-rewrite ignored under textured-shader state). Need v0.3 control-row bisect (xbed_load_default_shaders + per-cell SHADER_STAGE_PROGRAM writes) to isolate whether the bug is in the textured-shader state machine or the override path itself. **In parallel (per Codex finding 2):** add a sentinel combiner config to test whether row 2's `A_SOURCE=V0` rewrite is honored at all. Alternative path: `XEMU_METAL_DIAG_ATTRIB_DUMP=1` pipeline-key dump per draw to see if XBE's per-cell pipelines are being built.
- Codex review: COMPLETED. Verdict: **MINOR ISSUES** (3 findings, all adopted).
  - MEDIUM (adopted): Docs overstated what v0.2 ruled out; softened to "PASS_THROUGH-only explanation ruled out" across handoff, decision-log, manifest, orchestration state.
  - MEDIUM (adopted): Third live interpretation missing — added candidate (b) "combiner-rewrite ignored under textured-shader state" to the surviving list above and to the next-session bisect plan.
  - LOW (adopted): Orchestration docs were initially inconsistent about review state; all 6 docs synced to "COMPLETED, MINOR ISSUES" before commit.
  - OUT OF SCOPE (Codex confirmed): row-2 XBE wiring correct for the stated bisect; PASS_THROUGH-degraded-to-NONE analysis correct; queueing the psh.c fix as a separate slice is reasonable.
- Next proposed action: local commit (do NOT push); write Codex marker on post-commit clean fingerprint.
- Confidence / risk notes: HIGH that v0.2 is correctly built and runs on Metal as expected_fail (harness recognized the manifest declaration; harness sanity preserved from cycle 6 — same combiner-basic PASS would apply if rerun). HIGH that the bisect verdict refines v0.1 hypothesis 1 (PASS_THROUGH-specific reading ruled out; broader override-not-honored reading remains live). MEDIUM that the surviving candidates (a)/(b) will be narrowed within the next session by the v0.3 control-row bisect + combiner-sentinel instrumentation. HIGH that the separately-identified psh.c PASS_THROUGH bug is real (reading the source confirms it; Codex independently verified the trace), but LOW confidence that fixing it would not regress Metal boot stability without a dedicated test — hence queueing it as a separate slice.
