# Claude Status

- Objective: close Task #16 — land the two-bug fix scope (Metal renderer non-cubemap-2D `s.border` 2x-upload + xbed_texture library `BORDER_SOURCE_COLOR` default), rebuild affected XBE binaries, rebuild xemu, validate swizzle-mipmap on Metal with durable evidence, run Codex, update canonical docs, commit locally. **DONE — Exit Option A.**
- Outcome: **swizzle-mipmap PASSes byte-exact on Metal** (`changed_pixels_pct=0.0000`, `mean_abs_error=0.0000`, `rms_error=0.0000`, `max_abs_error=0`, `signal_match_pct=100.0000` at gate ≥95.0%). XBE flips from `expected_fail_renderers: ["xemu/gl", "xemu/metal"]` to `["xemu/gl"]` (GL leg still expected_fail for task #17). First-wave Metal XBE PASS count goes from 15/17 to 16/17.
- Files changed (uncommitted, ~58 LOC of functional code + 4 rebuilt XBE binary sets + doc + manifest updates):
  - `hw/xbox/nv2a/pgraph/mtl/texture_pg.c::decode_face_levels` (~12 LOC): adds `border_2d` branch to double `src_*` AND `dst_*` dims for non-cubemap-2D bordered swizzled path, mirroring `gl/texture.c:451-456` / `vk/texture.c:111`.
  - `hw/xbox/nv2a/pgraph/mtl/texture_pg.c::pgraph_mtl_texture_bind_from_pg` (~22 LOC): computes `border_2d_double` + `adjusted_width/height/texture_length` once; plumbed to dirty-range download/invalidate, cache lookup dims, and `pgraph_mtl_texture_bind_slot_cached_full` byte_length; surface fast path now guarded with `!border_2d_double` so bordered textures don't alias flat RT views.
  - `scripts/apple-silicon/xbe-tests/lib/xbed_texture.c::xbed_texture_bind_stage0` (~14 LOC incl. comment): adds `fmt |= XBED_FMT_BORDER_SOURCE_BIT;` so the composed format word sets `BORDER_SOURCE = COLOR` (bit 3 = 1), matching nxdk `samples/mesh/main.c:145` `0x0001122a`.
  - 4 XBE binary sets rebuilt: `swizzle-mipmap` + `texture-format-sweep` + `texture-filter-wrap` + `texture-dma-ab` (`.iso` + `bin/default.xbe` + 2 `main.{exe,obj}` per XBE where applicable).
  - `scripts/apple-silicon/xbe-tests/swizzle-mipmap/manifest.json`: `expected_fail_renderers` flipped, `expected_fail_notes` rewritten to describe closure.
  - Docs: `handoff.md` cycle-5 banner appended, `decision-log.md` cycle-5 entry appended, all four orchestration-state files reconciled.
- Commands/tests run this session: reads of handoff, decision-log, orchestration-state, automation flags context; inspection of `texture_pg.c` + `texture.mm` + `gl/texture.c` + `xbed_texture.{h,c}` to derive the fix; incremental `ninja -C build qemu-system-i386` (clean); `./build.sh -a arm64 --skip-shader-validation` (full Apple Silicon build successful + signed); `make clean && make` in 4 xbe-tests dirs (all clean); `xbe_orchestrator.py run` ×5 (swizzle-mipmap PASS + 3 LU_IMAGE_ regression PASS + 7 wider regress PASS + 5-XBE smoke 4/5 PASS + pipeline-smoke re-run confirms same fail mode); `/codex-validate changes` (MINOR ISSUES, 1 LOW finding adopted via revert of cosmetic `.inl` path noise, 1 open question deferred).
- Evidence produced (durable):
  - `benchmark-runs/20260522T054316Z-task16-swizzle-mipmap-validation/` — swizzle-mipmap canonical Metal PASS byte-exact (`summary.json` + `report.md` + `screenshots/swizzle-mipmap.0123.png` + compare-out).
  - `benchmark-runs/20260522T054422Z-task16-xbed-texture-regress/` — texture-format-sweep + texture-filter-wrap + texture-dma-ab all PASS on Metal (zero regression from library fix).
  - `benchmark-runs/20260522T055631Z-task16-wider-regress/` — 7/7 non-texture XBEs PASS on Metal (zero regression from renderer fix; msaa-aa-factor `not-built` is pre-existing).
  - `benchmark-runs/20260522T054657Z-task16-renderer-regress-smoke/` + `benchmark-runs/20260522T055508Z-task16-pipeline-smoke-recheck/` — pipeline-smoke FAIL pre-existing and deterministic; documented in handoff "Known pre-existing issue" section.
- Blockers / uncertainties: NONE for slice closure. The only known FAIL (pipeline-smoke) is independently verified as pre-existing and unrelated to this slice (Tier-4 CPU-painted XBE with no PGRAPH/texture interaction; root cause is xemu mutating `surface_scale=2` from harness's `surface_scale=1` toml).
- Next proposed action: commit (do NOT push). Codex validation complete; doc-sync done; orchestration-state reconciled; build clean; XBEs rebuilt.
- Confidence / risk notes: HIGH confidence in correctness — backed by (a) cross-renderer convention citation (gl/texture.c + vk/texture.c + mtl cubemap face-size already-doubles-but-crops), (b) byte-exact PASS on the originally-targeted test, (c) zero regressions across 11 other XBEs spanning texture / depth / stencil / quad / crtc / mirror / color / combiner / blend paths, (d) Codex independent review with no functional findings. LOW regression risk — renderer fix is gated on the precise `!s.cubemap && !f.linear && s.border` predicate (no existing code path active under those conditions was correct, since `decode_face_levels` would have under-read VRAM by 4× and `bind_slot_cached_full` would have keyed against wrong dims). Library fix is a no-op for the 3 LU_IMAGE_ users (gated out by `psh.c:179`) and a closure-positive change for the 1 SZ_ user (swizzle-mipmap).
- Codex review state: COMPLETED. Verdict: MINOR ISSUES (1 LOW finding adopted, 1 open question deferred). Marker `~/.claude/state/codex-validate-last-run` written post-revert. Safe to commit.
