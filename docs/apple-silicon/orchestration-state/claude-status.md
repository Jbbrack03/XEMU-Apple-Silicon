# Claude Status

- Objective: cycle 39 EEPROM scratchpad pre-`MmAllocateContiguousMemoryEx` discriminator — bounded implementation slice that ships the cycle-38-recommended Option C dynamic discriminator. Single-byte write at EEPROM offset `0xFF` inside `xbed_self_witness_fire`, gated AT MOST ONCE per process via a NEW sticky `s_eeprom_scratch_attempted` flag (so the breadcrumb is preserved across later fires even when allocation fails — Codex round-2 P1 finding adopted). Paired with two new oracle-agent verbs `eeprom.scratch.read` (4-branch decode that explicitly classifies non-`0xA4` `0xA?` values as "indeterminate" — Codex round-1 P2 finding adopted) and `eeprom.scratch.reset` (gated by existing `unsafe.enable`). The post-run byte on a future cycle-40 real-Xbox run uniquely discriminates G0 sub-case (c) "`MmAllocateContiguousMemoryEx` returned NULL silently" (byte=`0xA4` + WTNS count=0) from sub-cases (a)+(b) "crash before reaching the allocation call" (byte=`0x00` + WTNS count=0).
- Status: **CLOSED. Bounded implementation slice landed with Codex validation across 3 rounds.** Both source-side findings (R1 P2 + R2 P1) adopted with rebuilds; R3 returned no new cycle-39 issues (the round-3 P1 was about pre-existing tracked drift in `retail-*.py` importing the preserved-untracked `composite_preflight.py` — explicitly out of cycle-39 scope per the rolling cycle-34..38 Hermes-supervision guardrail; deflected with documented reason; the cycle-39 closing commit does NOT stage that drift). Codex marker written at `.claude/state/codex-validate-last-run` with the round-3 fingerprint. Both XBEs rebuilt cleanly (zero new warnings); witness-only/bin/default.xbe = 155 648 B SHA-256 `7528bb5bf4c9cc8f00c934c38e015166de7a6f89236ddc8e2ad2195a47ce4e0d`; oracle-agent/bin/default.xbe = 417 792 B SHA-256 `d419b452f127e5a065a51b2d5bba49b6f2ec1f1826cdec57845793c34440cb53`. handoff.md + decision-log.md cycle-39 entries on top with cycle-38 + cycle-37 + cycle-36 + cycle-35 preserved unchanged below.

## Why cycle 39 ran this session

Cycle 38 closure (commit `1127cafa0d`) exhausted the static binary surface as a discriminator for the 3 remaining G0 sub-cases: (a) crash inside nxdk's pre-`.CRT$X*` startup; (b) crash inside `_witness_only_pre_main_crt_xx`'s body before `MmAllocateContiguousMemoryEx`; (c) `MmAllocateContiguousMemoryEx` returns NULL silently. Cycle-38's closeout enumerated 3 cycle-39 candidates and recommended cycle-36 Option C (EEPROM scratchpad write inside `xbed_self_witness_fire` before the allocation call) as the lowest-cost option that uniquely splits (c) from (a)+(b); the other candidates (`out 0xe9` host-log breadcrumb — xemu-only, invisible on real Xbox; `.CRT$XCV` slot between `.CRT$XCU` and `main()` — adds no new info beyond cycle-35's stage-4 fire) were rejected with reasons. Cycle 39 executes that recommendation as a bounded implementation slice.

## What this session shipped

1. **EEPROM scratchpad contract.** Single byte at EEPROM offset `0xFF` (in the documented 0xC0..0xFF reserved/unused tail of the standard Xbox EEPROM; consistently zero on stock OEM consoles; 24LC02 endurance ≥1M cycles so one byte/run is bounded across hundreds of debugging sessions). Encoding: `0xA0 | (stage & 0x0F)` — high nibble `0xA` reuses existing "Path-A.4 tag" semantics; low nibble = stage. Real-Xbox first call into `xbed_self_witness_fire` is always the `.CRT$XXC` slot stage=4 fire (cycle 35 ordering) → expected post-run byte = `0xA4`.
2. **`lib/xbed_self_witness.h` — 3 new `#define`s + ~85-line cycle-39 head-comment addendum.** Constants `XBED_SELF_WITNESS_EEPROM_SMBUS_ADDR = 0xA8`, `XBED_SELF_WITNESS_EEPROM_SCRATCH_OFF = 0xFF`, `XBED_SELF_WITNESS_EEPROM_TAG_NIB = 0xA0`. Head-comment documents the scratchpad contract, discriminator semantics, fire-path gate rationale (with Codex P1 callout), reset/read semantics, and the EEPROM offset selection rationale.
3. **`lib/xbed_self_witness.c` — 1 new sticky-flag static + ~50-LOC code block.** New `s_eeprom_scratch_attempted` static int. New code block inside the existing `if (s_witness_page == 0)` first-call branch, gated by `if (!s_eeprom_scratch_attempted)` so it fires AT MOST ONCE per process regardless of allocation outcome. Flag is set BEFORE `HalWriteSMBusValue` (not only on `NT_SUCCESS`) so a write failure does not cause re-attempt. NTSTATUS host-logged in either branch (success or failure). Function continues to allocation attempt regardless of EEPROM-write outcome — purely additive instrumentation. Position: AS THE LAST INSTRUCTION before `MmAllocateContiguousMemoryEx`.
4. **`oracle-agent/commands.{c,h}` + `main.c` — 2 new verbs + dispatch entries + help-text rows + banner-comment.** `cmd_eeprom_scratch_read` (4-branch decode that distinguishes `0x00` baseline vs `0xA4` valid breadcrumb vs other-`0xA?` indeterminate vs else indeterminate — Codex round-1 P2 fix; returns line `off=0xFF byte=0xXX tag=0xX stage_nib=0xX interp="..."`) and `cmd_eeprom_scratch_reset` (writes `0x00` via `HalWriteSMBusValue`; gated by existing `s_unsafe_writes_enabled` — requires `unsafe.enable` first). Both registered in `s_cmds[]`; help-text rows added; banner updated to `v0.5 eeprom.scratch.*`.
5. **`witness-only/README.md` cycle-39 addendum (~120 LOC).** Scratchpad contract table (offset/SMBus address/encoded byte/write call/fire-path gate/failure handling/reset/read/endurance); cycle-40 G-row discriminator table (G0(a)+(b) / G0(c) / G1..G4 / indeterminate decode rows); cycle-40 11-step deployment runbook (extending cycle-36's with new step 3a `eeprom.scratch.reset` baseline + step 11 `eeprom.scratch.read` recovery bracketing the existing chainload window); design-choice rationale table (cycle-39 candidates per cycle-38 closeout); source impact + local validation evidence note.
6. **`witness-only/manifest.json` updated.** Title extended (`+ cycle-39 EEPROM scratchpad pre-MmAlloc discriminator`); new `real-xbox/physical/cycle-40` expected_results section with G-row enumeration + cycle-40 deployment notes.
7. **Build.** Both XBEs rebuilt cleanly via `eval "$(/Users/jbbrack03/XEMU_MacOS/nxdk/bin/activate -s)" && make` in both `witness-only/` and `oracle-agent/`; ZERO new lld warnings. Final SHA-256s recorded.
8. **Codex 3 rounds.** R1 P2 + R2 P1 both adopted with rebuilds; R3 P1 deflected (out-of-scope, pre-existing tracked drift). Marker written at `.claude/state/codex-validate-last-run`.
9. **Canonical docs/state synced.** handoff.md + decision-log.md cycle-39 entries on top with cycle-38 + cycle-37 + cycle-36 + cycle-35 preserved unchanged below; orchestration-state quartet (this file + current-cycle.md + validation-status.md + handoff-summary.md) closure pass.

## Session progress

- [x] Read required docs/state (CLAUDE.md, handoff.md cycle-38+37+36+35, decision-log.md cycle-38, orchestration-state quartet, `lib/xbed_self_witness.{c,h}`, witness-only/main.c, oracle-agent/{main,commands}.{c,h}, witness-only/README.md cycle-35 addendum + manifest.json).
- [x] Confirmed git state + pre-existing tracked drift + untracked `.hermes_*` + `composite_preflight.py` preserved un-staged per cycle-34+ prompt guardrail (carried forward through cycles 35 / 36 / 37 / 38 / 39).
- [x] Designed EEPROM scratchpad contract (offset 0xFF in reserved tail; encoded byte `0xA0 | stage_nib`; sub-case (c) discriminator only).
- [x] Implemented in `lib/xbed_self_witness.{c,h}` + `oracle-agent/{commands,main}.{c,h}`.
- [x] Rebuilt witness-only + oracle-agent; zero new warnings.
- [x] Local xemu cold-boot smoke ATTEMPTED but did NOT reach the XBE within bounded slice's wait budget — documented transparently; structural correctness coverage rests on clean link + cycle-29 shim's existing Codex-validated path + xemu QEMU smbus-eeprom support + Codex source review.
- [x] Updated `witness-only/README.md` + `manifest.json` with cycle-39 addendum + cycle-40 expected_results.
- [x] Codex round 1 (P2 reader 4-branch decode) adopted + rebuilt oracle-agent.
- [x] Codex round 2 (P1 sticky-flag gate to fire AT MOST ONCE per process) adopted + rebuilt witness-only.
- [x] Codex round 3 (P1 pre-existing tracked drift in retail-*.py) deflected with documented out-of-scope reason.
- [x] Codex marker written at `.claude/state/codex-validate-last-run`.
- [x] handoff.md cycle-39 entry on top above cycle-38.
- [x] decision-log.md cycle-39 entry on top above cycle-38.
- [x] Orchestration-state quartet closure pass.
- [x] Closure commit on `apple-silicon-performance` landed as `33fb5b7e34`; bounded doc-only / state-only closeout-sync follow-up commit (this slice) on top updates the orchestration-state quartet to reference the landed hash.

## Confidence + risk notes

- **HIGH confidence in EEPROM offset selection.** Offset 0xFF is in the documented 0xC0..0xFF reserved/unused tail of the standard Xbox EEPROM (consistently zero on stock OEM consoles); does NOT touch the factory/encrypted region 0x00..0x5F, the user-settings region 0x60..0x96, or the user-settings-checksum region 0x96..0xBF. 24LC02 endurance is ≥1M write cycles vs one write per cycle-39+ run.
- **HIGH confidence in sticky-flag gate correctness (Codex R2 P1 adopted).** The discriminator-breaking bug is now closed: the `s_eeprom_scratch_attempted` flag set BEFORE the write ensures the EEPROM byte is preserved across later fires regardless of `MmAllocateContiguousMemoryEx` outcome. This is the exact scenario the discriminator targets (G0(c) = allocation failure on first call); without the fix, later `.CRT$XCU` and in-main fires would overwrite `0xA4` → `0xA5`/`0xA1`/`0xA3` and destroy the signal.
- **HIGH confidence in reader 4-branch decode (Codex R1 P2 adopted).** `cmd_eeprom_scratch_read` now correctly classifies non-`0xA4` `0xA?` values as "indeterminate" per the cycle-40 G-row table row 4, rather than aliasing them onto sub-case (c).
- **MEDIUM confidence in cycle-40 outcome predictability.** Cycle 39 ships the instrumentation; the actual cycle-40 outcome is unknown until Hermes runs the real-Xbox sequence. The G-row table is exhaustive for the possible (byte, count) tuples but the actual cycle-40 result narrows the live G0 sub-case set (G0(c) → cycle 41 allocation-flag variations; G0(a)+(b) → cycle 41 requires custom XBE-header callback at significantly higher scope).
- **LOW risk to cycle-23 / cycle-27 / cycle-29 / cycle-31 / cycle-33 / cycle-35 / cycle-37 / cycle-38 prior guarantees.** ZERO source touched outside the cycle-29 shim's first-call branch (where the new code is additive and gated by a NEW sticky flag) + the new agent verbs (additive). Cycle-23 lockstep contract intact (no `xbed_a4_witness.{c,h}` change). Cycle-29 shim's allocation/stamp/wbinvd-flush path intact (the new EEPROM write is BEFORE the allocation; the existing post-allocation code path is structurally unchanged). Cycle-31 paint sequence intact (witness-only/main.c unchanged). Cycle-35 `.CRT$X*` slot mechanism intact (no main.c change).
- **NOTE on local validation.** Cold-boot xemu smoke did NOT reach the XBE within the bounded slice's wait budget — cold boot through BIOS to DVD load via the .app wrapper exceeds ~120 s without a pre-warmed snapshot. The cycle-35 closure's claimed "12 s spawn" almost certainly depended on a snapshot mechanism not available in this bounded session. Structural correctness coverage therefore rests on (i) clean nxdk lld link of both XBEs with zero new warnings, (ii) the cycle-29 shim's existing Codex-validated first-call branch (where the new EEPROM write is inserted as a sticky-flag-gated additive instruction sequence), (iii) xemu's QEMU `smbus-eeprom` device implementing both `eeprom_receive_byte` and `eeprom_write_data` per `hw/i2c/smbus_eeprom.c:52-83` (the SMBus write IS honored in emulation when reached, ruling out "broken in xemu" failure mode), (iv) Codex 3-round source review (2 source-side findings adopted with rebuilds).
- **R3 deflection rationale.** The round-3 P1 finding (about `retail-*.py` importing untracked `composite_preflight.py`) is correct on its merits but is about files explicitly preserved unstaged per the cycle-34..38 Hermes-supervision guardrail. The cycle-39 closing commit deliberately does NOT stage either the 4 tracked `retail-*` / `capture-composite-reference.sh` drift files OR the untracked `composite_preflight.py` file. The cycle-39 source surface itself (`lib/`, `oracle-agent/`, `witness-only/`) has no round-3 findings. Responsibility for resolving the tracked-drift dependency belongs to whichever future cycle commits those 4 files.

## What this session does NOT do

- NO host xemu source touched.
- NO `lib/xbed_a4_witness.{c,h}` touched (cycle-23 lockstep contract intact).
- NO `lib/lib.mk` touched.
- NO `lib/xbed_runtime.{c,h}` touched.
- NO `oracle-agent/{controller,smc,tier2,protocol}.{c,h}` touched.
- NO image-blit / pipeline-smoke / mirror / other diag XBE touched.
- NO `witness-only/main.c` touched (the new EEPROM-write is picked up via the cycle-35 `.CRT$X*` slot path automatically).
- NO `witness-only/Makefile` touched.
- NO `nxdk/` source touched.
- NO `tools/xemu-capture/` source touched.
- NO `composite-record.sh` / `composite-preflight.sh` source touched.
- NO net change to any flag default; M15 unchanged.
- NO real-Xbox run (cycle 40 is Hermes's call).
- NO PushNotification — bounded implementation slice; cycle-40 outcome may warrant one if it lands a clean G0(c) vs G0(a)+(b) split.
- NO cleanup of pre-existing untracked `.hermes_*` / `composite_preflight.py` files or pre-existing tracked drift in `capture-composite-reference.sh` / `retail-*.py` scripts (preserved per cycle-34+ guardrail).

## Next proposed action

Cycle 39 closes with the EEPROM scratchpad discriminator implementation landed + Codex-validated (2 rounds of source-side findings adopted with rebuilds; 1 round of out-of-scope finding deflected). The substantive next slice is cycle 40 (Hermes's call): deploy the cycle-39 oracle-agent + witness-only XBE on real Xbox via the cycle-40 11-step runbook in `witness-only/README.md`. Steps 3a (`unsafe.enable` + `eeprom.scratch.reset`) and 11 (`eeprom.scratch.read`) bracket the existing cycle-36 chainload window. The post-run EEPROM byte at offset `0xFF` is the primary cycle-40 signal; combine with `witness.scan-self count`/`reserved1` + composite stripes per the cycle-40 G-row table.
